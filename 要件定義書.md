# Marmo-Core 要件定義書

> 本書は**叩き台**であり、レビューにより継続的に改訂される。

## 1. 概要

### 1.1 プロダクトビジョン
**Marmo-Core** は、OpenClaw に代表される AI エージェント基盤を参考とした、**汎用 AI Agent Kernel（カーネル）** である。
特定のユースケースやモデルベンダーに依存せず、あらゆる AI エージェントの「中核実行エンジン」として機能することを目指す。最終的には世界中の開発者・組織に採用される OSS / 基盤ソフトウェアとなることを目標とする。

### 1.2 「Kernel」という位置づけ
OS におけるカーネルが「プロセス・メモリ・I/O・権限」を管理するのと同様に、Marmo-Core は AI エージェントの実行に必要な以下を抽象化・管理する。

- **タスク（実行計画）の管理** … OS のプロセススケジューラに相当
- **Skill / Tool（能力）の管理** … システムコール / デバイスドライバに相当
- **状態管理** … メモリ管理に相当
- **権限・安全性チェック** … 特権モード / アクセス制御に相当
- **外部環境との接続** … I/O サブシステムに相当
- **ログ・監査** … システムログ / 監査ログに相当

エージェントの「頭脳（LLM）」や「個性（プロンプト・ペルソナ）」は Kernel の外側（アプリケーション層）に置き、Marmo-Core はそれらを安全かつ確実に実行する**実行基盤レイヤ**に徹する。

### 1.3 設計原則
| 原則 | 内容 |
|------|------|
| モデル非依存 | 特定の LLM ベンダーに縛られない抽象化を提供する |
| 低依存・標準ライブラリ中心 | コアは外部ライブラリに依存しない。拡張機能のみ optional 依存として分離する |
| 安全第一 | あらゆる副作用を伴う操作は、権限・安全性チェックと監査の対象とする |
| 可観測性 | すべての判断・実行・状態遷移はログ化され、再現・追跡可能とする |
| 拡張性 | Skill / Tool / 外部接続はプラグインとして追加できる |
| 決定性と再現性 | 同一入力・同一状態からの実行は可能な限り再現可能とする |
| Human-in-the-loop | 重要操作には人間の確認を介在させられる |

### 1.4 ポジショニング・差別化
LangGraph / AutoGen / CrewAI / OpenAI Agents SDK / Semantic Kernel など先行プロダクトが存在する。Marmo-Core が世界的に採用されるための差別化軸を以下に定める。

| 差別化軸 | 内容 | 競合に対する優位 |
|----------|------|------------------|
| ゼロ依存コア | コアは標準ライブラリのみ。サプライチェーン攻撃面が極小で、監査・組込みが容易 | 重い依存ツリーを持つ既存フレームワークに対し、企業・組込み・エッジで採用しやすい |
| 安全性ファースト | Guard 必須通過・監査ログ・HITL を**設計の中心**に据える（後付けでない） | 「とりあえず動く」系に対し、本番・規制業界での信頼を獲得 |
| Kernel 抽象 | OS カーネル的に実行・権限・状態・I/O を分離。LLM/プロンプトは外側 | モデル・ベンダーロックインを回避、長期保守性 |
| 再現性 | イベントソーシング＋リプレイで実行を完全再現 | デバッグ・監査・評価が容易 |

**ポジショニング・ステートメント（案）**:
> Marmo-Core は、依存ゼロのコアと「安全性を設計の中心に据えた」アーキテクチャを持つ、ベンダー非依存の AI Agent Kernel である。プロトタイプから規制業界の本番運用まで、同じ基盤でスケールする。

> 注: 本節は製品の位置づけを示す前文であり、実装要件ではない。上記の差別化軸・比較表は README とドキュメント冒頭に反映し、競合状況に応じて継続的に更新する。

### 1.5 対象ユーザーと Time-to-first-success
OSS 普及の最大の予測因子は「導入から最初の成功体験までの速さ」である。

**対象ユーザー（ペルソナ）**:
- エージェント開発者（個人 / スタートアップ）— 速く作りたい
- プラットフォーム / 基盤エンジニア — 自社基盤に組み込みたい
- 規制業界・セキュリティ重視の企業 — 監査可能で安全な基盤が要る

**DX 要件**:
- **F-DX-01**: `pip install marmo-core` がゼロ外部依存で完了する。
- **F-DX-02**: 30 秒以内・最小コード行数で動く「Hello World エージェント」を提供する（コピペで動作）。
- **F-DX-03**: デフォルト設定で安全に動作する（secure by default。明示許可なしに副作用を起こさない）。
- **F-DX-04**: エラー・拒否メッセージは原因と次の一手を示す教育的な内容とする。
- **F-DX-05**: 代表ユースケースの動くサンプル集（cookbook）を同梱・公開する。

---

## 2. スコープ

### 2.1 本フェーズで実装する中核コンポーネント
本要件定義書では、以下の 9 つの中核コンポーネントを対象とする。

1. Skill / Tool 管理
2. Skill 検索・選択
3. 実行計画（プランニング）
4. 権限・安全性チェック
5. 状態管理
6. 失敗時のリカバリ
7. ログ・監査
8. 人間確認（Human-in-the-loop）
9. 外部環境との接続

実装の着手順・段階分けは「8. 実装フェーズと優先度」を参照。

### 2.2 本フェーズでは対象外（将来検討）
- LLM そのものの学習・ファインチューニング
- マルチエージェント間のオーケストレーション（将来の上位レイヤで扱う）
- GUI / 管理ダッシュボード（API・CLI を先行実装）
- 課金・マルチテナント基盤

---

## 3. アーキテクチャ概要

```
┌─────────────────────────────────────────────────────────────┐
│                  Application Layer（エージェント実装）          │
│        （LLM 呼び出し・プロンプト・ペルソナ・UI）              │
└───────────────────────────┬─────────────────────────────────┘
                            │  Kernel API
┌───────────────────────────▼─────────────────────────────────┐
│                       Marmo-Core Kernel                       │
│                                                               │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────────┐  │
│  │ 実行計画    │→ │ 権限・安全性 │→ │  Skill/Tool 実行ランタイム │  │
│  │ Planner    │  │ Guard       │  │  Executor               │  │
│  └────────────┘  └────────────┘  └────────────────────────┘  │
│        │               │                    │                 │
│  ┌─────▼───────┐  ┌────▼────────┐    ┌──────▼──────────────┐  │
│  │ Skill検索   │  │ 人間確認     │    │ 失敗時リカバリ        │  │
│  │ Registry/   │  │ HITL Broker │    │ Recovery Manager     │  │
│  │ Retriever   │  └─────────────┘    └─────────────────────┘  │
│  └─────────────┘                                              │
│  ┌─────────────────────┐  ┌──────────────────────────────┐   │
│  │ 状態管理 State Store │  │ ログ・監査 Audit Log         │   │
│  └─────────────────────┘  └──────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────┘
                            │  Connector API
┌───────────────────────────▼─────────────────────────────────┐
│         外部環境（API / DB / ファイル / ブラウザ / OS）        │
└───────────────────────────────────────────────────────────────┘
```

---

## 4. 機能要件

### 4.1 Skill / Tool 管理

#### 目的
エージェントが利用可能な「能力」（Skill / Tool）を統一的に登録・記述・管理する。

#### 要件
- **F-SKILL-01**: Skill / Tool を統一スキーマ（マニフェスト）で記述・登録できる。
- **F-SKILL-02**: マニフェストは少なくとも以下を含む。
  - 一意な識別子（name / id）、バージョン
  - 説明文（自然言語、検索・選択に利用）
  - 入力スキーマ（JSON Schema 等）と出力スキーマ
  - 副作用の種別（読み取り専用 / 書き込み / 外部送信 / 不可逆 など）
  - 必要権限（required permissions）
  - タグ・カテゴリ
- **F-SKILL-03**: Skill / Tool はプラグインとして動的に追加・無効化できる（ホットロード対応を望ましい要件とする）。
- **F-SKILL-04**: バージョン管理を行い、互換性のないバージョンを区別できる。
- **F-SKILL-05**: Skill（複数 Tool やロジックを束ねた高位の能力）と Tool（単一の実行可能関数）を区別して管理できる。
- **F-SKILL-06**: 各 Skill / Tool のメタデータ（成功率・平均実行時間・最終実行日時など）を蓄積し、選択や監査に利用できる。

#### 用語定義
- **Tool**: 単一の実行可能な関数・API 呼び出し。
- **Skill**: 1 つ以上の Tool・判断ロジックを束ねた、より高位の再利用可能な能力単位。

---

### 4.2 Skill 検索・選択

#### 目的
タスクや状況に応じて、最適な Skill / Tool を絞り込み・選択する。

#### 要件
- **F-SEARCH-01**: 自然言語のタスク記述から、関連する Skill / Tool を検索できる。
- **F-SEARCH-02**: 検索方式として以下をサポートする。
  - キーワード / タグによるフィルタ（MVP は標準ライブラリ `difflib` 等による語句類似で実現）
  - 説明文に対するセマンティック検索（ベクトル検索、埋め込みは差し替え可能なインターフェースとし、外部依存を要する実装は optional 拡張に分離する）
- **F-SEARCH-03**: 検索結果をスコアリングし、関連度順に並べられる。
- **F-SEARCH-04**: コンテキスト（現在の状態、利用可能な権限、過去の成功率）を考慮して候補を絞り込める。
- **F-SEARCH-05**: 大量の Skill が登録されても、LLM のコンテキストに渡す候補数を制御できる（Top-K 制限）。
- **F-SEARCH-06**: 選択結果とその理由をログに残せる（監査・改善のため）。

---

### 4.3 実行計画（プランニング）

#### 目的
与えられた目標（ゴール）を、実行可能なステップ列・依存グラフへ分解する。

#### 要件
- **F-PLAN-01**: 目標から実行計画（Plan）を生成できる。Plan は順序付きステップまたは DAG（有向非巡回グラフ）として表現する。
- **F-PLAN-02**: 各ステップは、使用する Skill / Tool、入力、期待される出力、依存関係を持つ。
- **F-PLAN-03**: Plan は実行前に検証される（未解決の依存・未許可の権限・存在しない Skill を検出）。
- **F-PLAN-04**: 実行中の結果に応じて Plan を再計画（re-plan）できる。
- **F-PLAN-05**: 並列実行可能なステップを識別し、依存関係を満たす範囲で並列化できる（望ましい要件。実現方式は「5. 実装方針」の並行モデルに従う）。
- **F-PLAN-06**: Plan は永続化され、中断・再開が可能（状態管理と連携）。
- **F-PLAN-07**: 計画方式は差し替え可能とする（LLM ベース / ルールベース / ハイブリッド）。

---

### 4.4 権限・安全性チェック（Guard）

#### 目的
副作用を伴う操作を実行前に検査し、許可されていない・危険な操作を防止する。

#### 要件
- **F-GUARD-01**: すべての Skill / Tool 実行は Guard を通過する（Guard をバイパスする経路を設けない）。
- **F-GUARD-02**: 権限モデルを定義する。
  - 操作ごとに required permissions を宣言（4.1 のマニフェスト）。
  - 実行コンテキストに付与された granted permissions と突き合わせる。
- **F-GUARD-03**: ポリシーエンジンにより、許可 / 拒否 / 人間確認要求（escalate）の判定を行える。ポリシーは宣言的に記述・差し替え可能とする。
- **F-GUARD-04**: 副作用の種別（読み取り / 書き込み / 外部送信 / 不可逆）に応じた段階的な制御を行う。不可逆・外部送信操作はデフォルトで人間確認を要求できる。
- **F-GUARD-05**: リソース制限（実行時間、呼び出し回数、コスト上限、レート制限）を設定・強制できる。
- **F-GUARD-06**: 危険操作（破壊的コマンド、機密情報の外部送信など）の検出ルールを持ち、ブロックまたは escalate する。
- **F-GUARD-07**: ドライラン（実際の副作用を起こさず計画・検証のみ行う）モードをサポートする。
- **F-GUARD-08**: すべての判定結果（許可 / 拒否 / escalate と理由）は監査ログに記録される。

---

### 4.5 状態管理（State Store）

#### 目的
エージェントの実行状態・コンテキスト・成果物を一貫して保持し、中断・再開・再現を可能にする。

#### 要件
- **F-STATE-01**: セッション / タスク単位で状態を保持する。状態には Plan・各ステップの実行結果・変数・メモリを含む。
- **F-STATE-02**: 状態は永続化でき、プロセス再起動後も再開できる。
- **F-STATE-03**: 状態遷移はイベントとして記録され、任意時点の状態を再構築できる（イベントソーシング的アプローチを推奨）。
- **F-STATE-04**: チェックポイントを作成し、特定時点へロールバックできる（失敗時リカバリと連携）。
- **F-STATE-05**: ストレージバックエンドは差し替え可能とする（インメモリ / ファイル / RDB / KVS。MVP は標準ライブラリの `json` ファイルまたは同梱の `sqlite3` を採用し、外部 DB は拡張とする）。
- **F-STATE-06**: 短期記憶（実行中コンテキスト）と長期記憶（セッションを跨ぐ知識）を区別して扱える。
- **F-STATE-07**: 機密データは原則として状態に保存しない。必要な場合は `SecretRef` 等の参照として扱い、ログ・LLM プロンプト・永続状態へ平文を混入させない。暗号化保存は State Store 実装または optional 拡張の責務とする。
- **F-STATE-08**: 並行アクセスに対する一貫性（楽観ロック / バージョニング）を保証する。

---

### 4.6 失敗時のリカバリ（Recovery Manager）

#### 目的
ステップ実行の失敗から、自動またはガイド付きで回復する。

#### 要件
- **F-RECOV-01**: 失敗を分類する（一時的エラー / 永続的エラー / タイムアウト / 権限拒否 / 検証失敗）。
- **F-RECOV-02**: リトライ戦略を設定できる（最大回数、指数バックオフ、ジッタ）。リトライ可否は失敗分類に基づく。
- **F-RECOV-03**: 失敗時に代替 Skill / Tool へフォールバックできる。
- **F-RECOV-04**: チェックポイントへロールバックし、再計画（4.3 F-PLAN-04）して再実行できる。
- **F-RECOV-05**: 補償処理（compensation / saga パターン）を定義し、既に発生した副作用を取り消せる（取り消し可能な操作について）。
- **F-RECOV-06**: 自動回復不能と判断した場合は、人間確認（4.8）へエスカレーションする。
- **F-RECOV-07**: リカバリの全過程（失敗内容・採用した戦略・結果）を監査ログに記録する。
- **F-RECOV-08**: 無限ループ・暴走を防ぐサーキットブレーカー（連続失敗時の停止）を備える。

---

### 4.7 ログ・監査（Audit Log）

#### 目的
すべての判断・実行・状態遷移・人間介入を記録し、再現性・説明可能性・コンプライアンスを担保する。

#### 要件
- **F-LOG-01**: 構造化ログ（機械可読、例: JSON）で出力する（MVP は標準 `logging` + JSON フォーマッタで実現）。
- **F-LOG-02**: 記録対象は少なくとも以下を含む。
  - 実行計画の生成と再計画
  - Skill / Tool の選択理由と実行（入力・出力・所要時間・結果）
  - 権限・安全性チェックの判定と理由
  - 状態遷移・チェックポイント
  - 失敗とリカバリ
  - 人間確認の要求・応答
  - 外部環境との通信
- **F-LOG-03**: 各レコードは一意の相関 ID（trace / span）でひも付けられ、1 タスクの全フローを追跡できる。
- **F-LOG-04**: 監査ログは改竄検知可能とする（追記専用 / ハッシュチェーンを望ましい要件とする。`hashlib` で実現可能）。
- **F-LOG-05**: 機密情報（資格情報・個人情報）はマスキング / 編集して記録する。
- **F-LOG-06**: ログレベル・出力先（標準出力 / ファイル / 外部監視基盤）を設定できる。
- **F-LOG-07**: ログから実行をリプレイ・検証できる（再現性、4.5 F-STATE-03 と連携）。
- **F-LOG-08**: メトリクス（成功率・レイテンシ・コスト・トークン使用量）を集計・エクスポートできる（OpenTelemetry 等への対応は optional 拡張とする）。

---

### 4.8 人間確認（Human-in-the-loop / HITL Broker）

#### 目的
重要・危険・不確実な操作について、人間の承認・入力・判断を介在させる。

#### 要件
- **F-HITL-01**: Guard やリカバリからのエスカレーションを受け、人間への確認要求を生成できる。
- **F-HITL-02**: 確認要求には、対象操作・想定される影響・代替案・推奨判断を含める。
- **F-HITL-03**: 応答種別として、承認 / 拒否 / 修正（入力差し替え） / 保留 をサポートする。
- **F-HITL-04**: 確認待ちの間、対象タスクは状態を保存して安全に一時停止し、応答後に再開できる（4.5 と連携）。
- **F-HITL-05**: 確認の通知・応答チャネルは差し替え可能とする（CLI / API / Webhook / チャット等）。
- **F-HITL-06**: タイムアウト時のデフォルト挙動（拒否 / 保留継続）を設定できる。
- **F-HITL-07**: 承認ポリシーを設定できる（特定操作は常に確認、特定ユーザのみ承認可、など）。
- **F-HITL-08**: すべての確認要求と応答（誰が・いつ・何を）を監査ログに記録する。

---

### 4.9 外部環境との接続（Connector）

#### 目的
ファイル・API・DB・ブラウザ・OS など外部環境への接続を、安全かつ統一的に抽象化する。

#### 要件
- **F-CONN-01**: 外部接続を Connector として抽象化し、統一インターフェースで扱う。
- **F-CONN-02**: 標準的な Connector 種別を想定する（HTTP/REST API、ファイルシステム、シェル / OS コマンド、データベース、ブラウザ自動化、メッセージング）。MVP は標準ライブラリのみで実現できる種別（`urllib`=HTTP、ファイル、`subprocess`=シェル、`sqlite3`=DB）を優先する。
- **F-CONN-03**: Connector はプラグインとして追加できる（4.1 の Skill / Tool 管理と整合）。
- **F-CONN-04**: 外部接続は必ず Guard（4.4）の権限・安全性チェックを経る。
- **F-CONN-05**: 資格情報（API キー・トークン）は秘匿管理し、ログに平文で出さない（シークレットストア連携を想定）。
- **F-CONN-06**: タイムアウト・リトライ・レート制限・サーキットブレーカーを Connector レベルで設定できる。リトライ / サーキットブレーカーの戦略はリカバリ（4.6）と、リソース / コスト上限は Guard（4.4 F-GUARD-05）と整合させ、本要件は Connector 単位の設定点を提供することに限定する。
- **F-CONN-07**: サンドボックス / 隔離環境での実行をサポートする（特にシェル・ブラウザなど高リスク接続）。隔離レベル（L0〜L3）の定義・宣言・検査はセキュリティ節（4.12 F-SEC-05）を正本とし、本要件はそれに従う。
- **F-CONN-08**: 外部とのすべての送受信は監査ログに記録される（機密はマスキング）。
- **F-CONN-09**: 標準プロトコル（例: MCP = Model Context Protocol）との相互運用を将来的にサポートできる拡張余地を持つ。

---

### 4.10 LLM アダプタ層（LLM Provider）

#### 目的
LLM 本体はカーネル外（アプリ層）に置くという原則を保ちつつ、ユーザーが最短で LLM に接続できる**標準アダプタ interface と参照実装**を提供する。「で、結局どう繋ぐのか」という採用障壁を取り除く。

#### 要件
- **F-LLM-01**: LLM 接続を抽象 interface（`LLMProvider`）として定義する。コアはこの interface のみに依存し、具象実装は持たない。
- **F-LLM-02**: 参照実装として、API経由のLLM（OpenAI / Anthropic 等）とローカルLLMの両方を **optional extra** として提供し、ユーザーが用途に応じて両者をシームレスに選択・併用できるようにする。未導入でもコアは動作し、ゼロ依存を維持する。
- **F-LLM-03**: interface は少なくとも、チャット補完・ツールコール・ストリーミング・トークン使用量の返却を抽象化する。
- **F-LLM-04**: テスト・リプレイ用のモック / 決定論的 LLM 実装を**ゼロ依存で同梱**する（F-DX・再現性と連携）。
- **F-LLM-05**: ツールコール形式（OpenAI / Anthropic 等）と Skill マニフェスト（4.1）を相互変換できる。
- **F-LLM-06**: トークン使用量・コストを監査ログ（F-LOG-08）・Guard のコスト上限（F-GUARD-05）へ受け渡す。
- **F-LLM-07**: アプリケーション利用者は、プロバイダーごとの API 仕様・ツールコール形式・ストリーミング形式・エラー形式の差分を原則として意識せず、統一された `LLMProvider` interface を通じて利用できる。各プロバイダー固有の差分はアダプタ層で吸収し、必要な場合のみ provider-specific option として明示的に扱う。

---

### 4.11 Skill の配布・パッケージング（Distribution / Registry）

#### 目的
カーネルのインパクトは**プラグイン・エコシステムの大きさ**で決まる。Skill / Tool / Connector を共有・配布・再利用するための規約を定める（本製品の主要な勝ち筋）。

#### 要件
- **F-DIST-01**: Skill パッケージ形式を定義する（マニフェスト + 実装 + 依存宣言 + メタデータ）。
- **F-DIST-02**: 名前空間とバージョニング（SemVer）を持ち、互換性を判定できる。
- **F-DIST-03**: 検出・取得元として、ローカルディレクトリを最小実装でサポートする。Git リポジトリからの取得と集中レジストリは、Phase 1 以降の拡張として扱う。
- **F-DIST-04**: パッケージの改竄検知として、MVP ではハッシュ固定・ロックファイル・取得元記録を提供する。暗号学的な署名・検証は標準ライブラリだけで自前実装せず、将来の optional 拡張または外部ツール連携で扱う。
- **F-DIST-05**: 各パッケージは対応するカーネルバージョン範囲を宣言し、非互換を導入前に検出する。
- **F-DIST-06**: MCP サーバー（F-CONN-09）を Skill 供給源として取り込むアダプタを将来サポートする。
- **F-DIST-07**: 公開・共有を促すドキュメント（Skill 作成ガイド・テンプレート）を整備する。

---

### 4.12 セキュリティと脅威モデル

#### 目的
AI エージェントは外部の信頼できない内容（Web・ファイル・ツール出力）を読み、それに基づいて副作用のある操作を行う。**プロンプトインジェクション**（注入された指示による意図しない操作・情報漏洩）はエージェント最大のリスクであり、カーネルの設計中心に据える。Guard（4.4）が「権限の検査」を担うのに対し、本節は「信頼できない内容由来の指示」への対処を担う。

#### 想定脅威（脅威モデル）
- プロンプトインジェクション（直接 / 間接：取得したコンテンツに埋め込まれた指示）
- 機密情報の外部送信（データ持ち出し / exfiltration）
- 悪意ある Skill / Connector / パッケージ（供給網）
- 過剰権限・権限昇格
- 破壊的・不可逆操作の誘発
- 資格情報・秘密情報の漏洩

#### 要件
- **F-SEC-01**: データの出所を信頼境界でラベリングする（信頼できる指示 / 信頼できない内容）。ツール出力・外部取得コンテンツはデフォルトで「信頼できない」として扱う。
- **F-SEC-02**: 信頼できない内容に含まれる「指示らしき文字列」を、実行すべき指示として自動昇格させない（content と instruction の分離）。
- **F-SEC-03**: 副作用操作は宣言された Plan / 許可済み Skill の範囲に限定し、信頼できない内容が新たな副作用操作を直接誘発できないようにする。
- **F-SEC-04**: データ持ち出し防止。外部送信先のアロー/ブロックリスト、機密データを外部送信操作へ渡す際の検出と escalate（4.4 / 4.8 連携）。
- **F-SEC-05**: 実行隔離（サンドボックス）の保証レベルを定義し、Connector ごとに宣言する（F-CONN-07 を具体化）。Core は隔離レベルの宣言・検査・監査を担い、OS レベルの強い隔離そのものは環境または optional 拡張の責務とする。
  - L0: 隔離なし（読み取り専用・低リスクのみ）
  - L1: プロセス分離・リソース制限（`subprocess` + タイムアウト / リソース上限）
  - L2: ファイルシステム / ネットワークの制限（隔離ディレクトリ・許可ホストのみ）
  - L3: コンテナ / VM 等の強隔離（高リスク：シェル・ブラウザ。外部依存可の optional 拡張）
- **F-SEC-06**: 秘密情報（資格情報・トークン）は `SecretRef` として参照し、状態・ログ・LLM プロンプトへ平文で混入させない（4.5 F-STATE-07 / 4.7 F-LOG-05 と整合）。外部 Secret Store 連携や暗号化保存は optional 拡張とする。
- **F-SEC-07**: 最小権限をデフォルトとし、Skill / Connector は宣言した required permissions の範囲でのみ動作する。
- **F-SEC-08**: 脅威モデルと対策を `docs/threat-model` に文書化し、リリースごとに見直す。
- **F-SEC-09**: インジェクション・データ持ち出しの試行を検出した場合は監査ログに記録し、必要に応じて人間確認へ escalate する。

> 注: カーネルはプロンプトインジェクションを完全に「防止」できないことを前提に、**被害を局所化する**（最小権限・隔離・人間確認・監査）多層防御の立場をとる。

---

## 5. 実装方針（Python / 低依存）

### 5.1 言語・ランタイム
- **言語**: Python。型ヒント・`dataclasses`・`match` 文・`asyncio` の TaskGroup を活用するため、サポート下限を **Python 3.11** とする（要レビュー）。
- **コアの外部依存**: なし。標準ライブラリのみでカーネル中核を構成する。
- **拡張の依存**: セマンティック検索・外部 DB・ブラウザ自動化・OpenTelemetry 等、外部ライブラリを要するものは optional extra（例: `marmo-core[semantic]`）として本体から分離する。

### 5.2 標準ライブラリへのマッピング（MVP のゼロ依存実装方針）
ゼロ依存は「Core が追加パッケージなしで安全な最小機能を提供する」ための方針であり、暗号化・署名・強隔離のような安全保証を標準ライブラリだけで無理に自前実装することを意味しない。Core は以下を担う。

- 機密値を平文で保持・出力しないための `SecretRef`、マスキング、ログ編集。
- 改竄検知のためのハッシュ、ロックファイル、取得元・バージョンの記録。
- Connector / Skill が要求する隔離レベルの宣言、Guard による検査、監査ログへの記録。

一方で、暗号化保存、暗号学的署名検証、コンテナ / VM 等の強隔離、外部 Secret Store 連携は optional extra または利用環境側の責務とする。

| 要件 | 一般的な外部依存 | MVP（標準ライブラリ）方針 |
|------|----------------|--------------------------|
| Skill セマンティック検索 (F-SEARCH-02) | numpy / FAISS / 埋め込み SDK | まず `difflib` による語句類似。ベクトル検索はインターフェースのみ定義し後付け |
| 状態永続化 (F-STATE-05) | SQLAlchemy / Redis | `json` + ファイル、または同梱の `sqlite3` |
| 構造化ログ (F-LOG-01) | structlog / loguru | 標準 `logging` + JSON フォーマッタ |
| 改竄検知 (F-LOG-04) | 専用ライブラリ | `hashlib` によるハッシュチェーン |
| メトリクス (F-LOG-08) | OpenTelemetry SDK | インターフェースのみ定義、実装は optional 拡張 |
| HTTP 接続 (F-CONN-02) | requests / httpx | `urllib.request` |
| シェル接続 (F-CONN-02) | — | `subprocess` |
| 入出力スキーマ検証 (F-SKILL-02) | jsonschema / pydantic | `dataclasses` + 自前バリデータ（軽量 JSON Schema サブセット） |
| 機密情報管理 (F-STATE-07 / F-SEC-06) | keyring / Vault SDK / KMS SDK / cryptography | Core は `SecretRef` とマスキングのみ。暗号化保存・外部 Secret Store は optional 拡張 |
| パッケージ署名 (F-DIST-04) | sigstore / GPG / cryptography | MVP はハッシュ固定・ロックファイル・取得元記録。署名検証は optional 拡張 |
| 強隔離 (F-CONN-07 / F-SEC-05) | Docker / Firecracker / sandbox ランタイム | Core は隔離レベル宣言・Guard 検査・監査のみ。L3 実行は optional 拡張 |

### 5.3 並行モデル
- 外部接続待ち（I/O バウンド）が処理の大半を占めるため、**`asyncio` を中核**に据える。標準同梱であり追加依存が不要。
- 並列実行可能なステップ（F-PLAN-05）は依存解決後に `asyncio.gather` / `TaskGroup` で並行実行する。
- ブロッキング処理・CPU バウンド処理（一部 Connector）は `loop.run_in_executor`（`ThreadPoolExecutor`）へ退避する。
- 同期 API も提供し、`asyncio.run` でラップして同期利用できるようにする。

### 5.4 拡張インターフェースの方針
- 各エンジン（Planner / Retriever / Guard / StateStore / Recovery / AuditLog / HITLBroker / Connector）は `abc.ABC` による抽象基底クラスで定義し、差し替え可能とする。
- カーネルは具象実装を直接 import せず、登録（依存性注入）で受け取る。

---

## 6. Kernel 公開 API（概念設計）

アプリケーション層は以下の最小 API でカーネルを駆動する（シグネチャは概念であり確定ではない）。

```python
class Kernel:
    # 登録（プラグイン注入）
    def register_skill(self, manifest: SkillManifest, impl: Callable) -> None: ...
    def register_connector(self, name: str, connector: Connector) -> None: ...
    def set_policy(self, policy: Policy) -> None: ...
    def set_planner(self, planner: Planner) -> None: ...

    # 実行ライフサイクル
    async def submit(self, goal: Goal, context: ExecutionContext) -> TaskId: ...
    async def run(self, task_id: TaskId) -> TaskResult: ...      # 完了 or 一時停止まで進める
    async def resume(self, task_id: TaskId,
                     hitl_response: HitlResponse | None = None) -> TaskResult: ...
    async def cancel(self, task_id: TaskId) -> None: ...

    # 観測
    def get_state(self, task_id: TaskId) -> TaskState: ...
    def subscribe(self, handler: Callable[[Event], None]) -> None: ...  # ログ/進捗イベント
```

`run` はゴール完了・失敗・**人間確認待ちでの一時停止**のいずれかで返る。一時停止時はアプリ層が応答を集め、`resume` で再開する（F-HITL-04 / F-STATE-02）。

---

## 7. データモデル例（概念）

確定スキーマではなく、各コンポーネント間で受け渡す主要構造の骨子。

```python
@dataclass
class SkillManifest:                       # 4.1
    id: str
    version: str
    description: str
    input_schema: dict                     # 軽量 JSON Schema サブセット
    output_schema: dict
    side_effect: Literal["read", "write", "external", "irreversible"]
    required_permissions: list[str]
    tags: list[str]

@dataclass
class Step:                                # 4.3
    id: str
    skill_id: str
    inputs: dict
    depends_on: list[str]                  # 依存ステップ ID（DAG）
    status: Literal["pending", "running", "done", "failed", "skipped"]

@dataclass
class Plan:                                # 4.3
    goal: str
    steps: list[Step]

@dataclass
class GuardDecision:                       # 4.4
    verdict: Literal["allow", "deny", "escalate"]
    reason: str

@dataclass
class AuditRecord:                         # 4.7
    trace_id: str
    span_id: str
    timestamp: str
    kind: str                              # plan / select / guard / exec / state / recovery / hitl / connector
    payload: dict                          # 機密はマスキング済み
    prev_hash: str | None                  # ハッシュチェーン（改竄検知）
    hash: str
```

---

## 8. 実装フェーズと優先度

MoSCoW を併記する（M=Must / S=Should / C=Could）。各フェーズは**エンドツーエンドで動く**ことを区切りとする。

### 8.1 MVP の境界
Phase 0 は「Kernel として成立する最小実行ループ」に限定する。目的は、広い機能を浅く実装することではなく、以下の不変条件を早期に固定することである。

- 登録済み Tool だけを実行できる。
- Tool 実行は必ず Guard を通過する。
- 実行結果・Guard 判定・状態遷移は必ず Audit Log に残る。
- 副作用のある Tool は明示権限がない限り拒否される。
- モック LLM / ルールベース Planner により、外部依存なしで再現可能な Hello World が動く。

Phase 0 では、以下を対象外とする。

- DAG 並列実行、再計画、フォールバック、補償処理。
- 実 LLM Provider、実外部 API Connector、ブラウザ自動化。
- Human-in-the-loop の通知チャネル実装。
- Skill 配布、Git 取得、集中レジストリ、署名検証。
- 暗号化保存、外部 Secret Store、コンテナ / VM 等の強隔離。
- OpenTelemetry 連携、サービス / デーモン運用、水平スケール。

Phase 0 で満たす範囲は、全体要件の完全実装ではなく、将来拡張しても破壊しない中核契約の固定とする。特に以下を Phase 0 の扱いとして明確化する。

- **State Store**: `StateStore` interface と `InMemoryStateStore` を必須とする。プロセス再起動後の再開（F-STATE-02）やファイル / DB 永続化は、Phase 0 では実装してもよいが必須完了条件とはしない。
- **Audit Log**: 構造化された `AuditRecord` と追記可能な `AuditLog` interface を必須とする。ハッシュチェーンによる改竄検知（F-LOG-04）は、データモデル上の拡張余地を確保しつつ、Phase 0 の必須完了条件からは外す。
- **HITL / Recovery / Connector**: Phase 0 では interface または将来接続点の設計に留め、通知チャネル、リトライ、外部 API、シェル、ブラウザ等の実装は Phase 1 以降とする。
- **LLM Provider**: Phase 0 では決定論的モック / ルールベース Planner を対象とし、実プロバイダーの参照実装とプロバイダー差分吸収は Phase 1 以降で実装する。ただし `LLMProvider` interface は Phase 0 から破壊的変更を避ける前提で設計する。

| Phase | 含む要件 | 優先度 | ゴール |
|-------|---------|--------|--------|
| **Phase 0 — MVP** | Tool 登録と実行(4.1 の最小)、線形 Plan(4.3 の最小)、基本 Guard(4.4)、インメモリまたは json 状態管理(4.5 の最小)、構造化 Audit Log(4.7 の最小)、モック LLM / ルールベース Planner(4.10 F-LLM-04)、Hello World/DX(1.5) | M | 登録済み Tool を 1 つ以上含む線形 Plan を実行し、Guard 判定・状態遷移・Audit Log まで通る。外部依存なしで 30 秒以内に成功体験を得られる |
| **Phase 1 — 実用化** | キーワード検索(4.2)、失敗リカバリ(4.6 の基本)、人間確認(4.8 の基本)、基本 Connector(4.9: HTTP/ファイル/シェル)、LLM Provider interface と参照実装(4.10 F-LLM-01/02)、Skill 配布 最小=ローカル(4.11)、**プロンプトインジェクション/隔離 基本対策(4.12)**、**MCP 相互運用 interface(4.9 F-CONN-09)**、**メトリクス/OTel interface(4.7 F-LOG-08)** | M/S | 外部環境・実 LLM に接続し、失敗回復と人間承認を経て実タスクを安全に完遂。標準連携・観測の土台が入る |
| **Phase 2 — 高度化** | セマンティック検索(4.2)、DAG 並列実行(4.3 F-PLAN-05)、チェックポイント/ロールバック(4.5 F-STATE-04)、補償処理(4.6 F-RECOV-05)、Skill レジストリ・署名(4.11)、強隔離 L3(4.12 F-SEC-05)、サービス化/水平スケール等の運用要件(12 / 15 から退避分) | S/C | 規模・並列度・堅牢性・エコシステム・運用性の拡大 |
| **Phase 3 — エコシステム** | MCP を Skill 供給源として取込(4.11 F-DIST-06)、マルチエージェント、財団移管・公開レジストリ運用 | C | 上位レイヤとエコシステム拡大 |

> Tier2 レビューでの前倒し: MCP 相互運用 interface と OTel interface は信頼性・標準連携の前提条件のため Phase 3 → **Phase 1** へ。プロンプトインジェクション対策(4.12)は Must として Phase 1 に含める。MCP を Skill 供給源として取り込む実装は Phase 3 に残す。

---

## 9. 非機能要件

| 区分 | 要件 |
|------|------|
| 低依存 | コアは標準ライブラリのみ。外部依存は optional extra に隔離し、未導入でもコアが動作する |
| 拡張性 | Skill / Tool / Connector / 各エンジンはプラグイン・差し替え可能なインターフェースとする |
| 移植性 | 特定の LLM・クラウド・言語ランタイムにロックインされない設計とする |
| 可観測性 | すべての主要動作はログ・メトリクス・トレースで観測可能とする |
| 安全性 | デフォルトで最小権限（least privilege）。副作用は明示的許可がない限り実行しない |
| 信頼性 | 中断・再開・リカバリにより、部分的失敗から回復できる |
| 性能 | I/O バウンド処理を asyncio で並行化し、依存のないステップを並列実行できる |
| セキュリティ | 機密情報の秘匿管理・マスキング・改竄検知ログを備える |
| テスト容易性 | ドライラン・モック Connector・リプレイにより、副作用なしで検証できる |
| ドキュメント | OSS として採用されるために、API・拡張方法を体系的に文書化する（英日併記） |

---

## 10. 主要な相互作用（実行フロー例）

1. アプリケーション層がゴールを Kernel に投入する（`submit`）。
2. **実行計画**（4.3）がゴールをステップ列 / DAG に分解する。
3. 各ステップで **Skill 検索・選択**（4.2）が候補を絞り、使用する Skill / Tool を決定する。
4. 実行前に **権限・安全性チェック**（4.4）が判定する。
   - 許可 → 実行へ
   - escalate → **人間確認**（4.8）へ（タスクは一時停止し `resume` を待つ）
   - 拒否 → ステップ失敗
5. **Connector**（4.9）経由で **外部環境** と接続し、Skill / Tool を実行する。
6. 結果は **状態管理**（4.5）に記録され、チェックポイントが作られる。
7. 失敗時は **リカバリ**（4.6）がリトライ / フォールバック / 再計画 / エスカレーションを行う。
8. 全工程は **ログ・監査**（4.7）に相関 ID 付きで記録される。

---

## 11. 用語集

| 用語 | 定義 |
|------|------|
| Kernel | エージェント実行の中核基盤。Marmo-Core 本体。 |
| Tool | 単一の実行可能な関数・API 呼び出し。 |
| Skill | 複数 Tool・判断ロジックを束ねた高位の再利用可能な能力。 |
| Plan | ゴールを分解した実行計画（ステップ列 / DAG）。 |
| Guard | 権限・安全性チェックを行うコンポーネント。 |
| Connector | 外部環境への接続を抽象化したコンポーネント。 |
| HITL | Human-in-the-loop。人間の確認・介入を介在させる仕組み。 |
| Checkpoint | 任意時点の状態スナップショット。ロールバックに使用。 |
| Escalate | 自動判断せず人間確認へ引き上げること。 |
| optional extra | コアに含めず、必要時のみ追加インストールする外部依存つき拡張。 |

---

## 12. 今後の拡張（参考）

2.2「対象外」と重複しない範囲で、将来レイヤとして想定する拡張を記す。

**上位レイヤ / エコシステム**
- マルチエージェント・オーケストレーション層
- 管理ダッシュボード / 可視化 UI
- MCP をはじめとする標準プロトコルとの完全な相互運用
- マルチテナント・課金基盤
- 学習ループ（実行結果から Skill 選択ポリシーを改善）

**運用・スケール（15章から退避。利用実態が固まる Phase 2 以降）**
- サービス / デーモン化、コンテナ公式イメージの提供（F-OPS-01 の拡張形態）
- 単一インスタンスでの複数タスク / 複数エージェント同時実行
- 共有 State Store を介した複数インスタンスでの水平スケール（占有・再開はロック / バージョニング F-STATE-08 による）
- ヘルスチェック / readiness、グレースフルシャットダウン等の運用エンドポイント
- 永続状態のスキーマ・マイグレーション手順の整備（F-OPS-03 の具体化）

---

## 13. 未決事項（要レビュー）
- Python サポート下限（3.11 を仮置き。3.10 まで下げるか）
- 入出力スキーマに JSON Schema フルではなく軽量サブセットを自前実装する範囲
- 状態永続化の MVP を `json` ファイルと `sqlite3` のどちらで始めるか
- 監査ログのハッシュチェーンを Phase 0 から入れるか Phase 1 に回すか
- 同期 API と非同期 API のどちらを「主」とするか

---

## 14. ライセンス・ガバナンス・コミュニティ

世界的貢献者を集め、企業に安心して採用されるための非技術的基盤。技術仕様と同等に重要。

### 14.1 ライセンス
- **Apache License 2.0 を推奨**（要確定）。明示的な特許条項を持ち、企業の法務審査を通りやすく、基盤 OSS の事実上の標準。
- MIT は許容度が高いが特許条項がない。基盤・エージェント領域では Apache 2.0 の方が企業採用上有利。
- **F-GOV-01**: ライセンスをリポジトリルートの `LICENSE` に明記し、全配布物に適用する。

### 14.2 貢献・コミュニティ
初期は **BDFL / コア小チーム**で意思決定を高速化する。

- **F-GOV-02**: `CONTRIBUTING.md`（開発手順・コーディング規約・PR フロー）を整備する。
- **F-GOV-03**: `CODE_OF_CONDUCT.md`（行動規範）を整備する。
- **F-GOV-04**: **DCO（Developer Certificate of Origin / sign-off）** を採用し、貢献の出自を明確化する（CLA より貢献障壁が低い）。
- **F-GOV-05**: Issue / PR テンプレート、good-first-issue ラベル運用で新規貢献者を受け入れる。

### 14.3 API 安定性
- **F-GOV-06**: 公開 API・Skill マニフェストスキーマに **SemVer** を適用し、メジャーバージョン内では後方互換を破壊しない。stable / experimental の階層分けと非推奨ポリシー（猶予期間・移行ガイド）の整備は、公開 API が安定し始める段階で詳細化する。

### 14.4 セキュリティ運用
- **F-GOV-07**: `SECURITY.md` に脆弱性開示窓口と対応プロセスを明記する。

### 14.5 採用拡大後に検討する非技術基盤
以下は採用・貢献者が一定規模に達した段階で着手する。現フェーズでは方向性の記録に留め、要件としては固定しない。

- **ガバナンスの正式化**: ステアリング委員会 → 中立財団（例: LF AI & Data Foundation）への移管を視野に入れ、`GOVERNANCE.md` を整備する。中立性は大企業の貢献・依存を促す。
- **変更管理（RFC）**: 破壊的変更・新コンポーネント・公開 API 変更について、経緯を公開で残す RFC プロセスを導入する。
- **供給網の信頼性**: 署名付きリリース・SBOM 提供を整備する（F-DIST-04 と整合）。
- **透明性**: 公開ロードマップ（GitHub Projects / Discussions 等）でコミュニティ形成を促進する。

### 14.6 リポジトリ必須ファイル（チェックリスト）
現フェーズで必須: `README.md`（英日併記）/ `LICENSE` / `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md` / `SECURITY.md` / `CHANGELOG.md` / Issue・PR テンプレート / `要件定義書.md`（本書）
採用拡大後に追加: `GOVERNANCE.md`（14.5）

---

## 15. デプロイ形態

カーネルがまず「組込みライブラリ」として採用されるための最小限の形態・設定要件のみを定める。サービス / デーモン化、水平スケール、運用エンドポイント等の本格的な運用要件は、利用実態が固まる Phase 2 以降の検討事項とし、本節では要件化しない（「12. 今後の拡張」参照）。

- **F-OPS-01**: 第一の利用形態として**組込みライブラリ**（同一プロセス内、ゼロ依存・最小構成）をサポートする。スタンドアロン・サービス / デーモン、コンテナ公式イメージは将来の拡張形態とする。
- **F-OPS-02**: 設定は環境変数 / 設定ファイルで与えられ、コード変更なしに切替できる（12-factor 準拠）。
- **F-OPS-03**: 永続化された状態（4.5）にスキーマバージョンを持たせ、カーネル更新時に移行可能な余地を残す（マイグレーション手順の整備は Phase 2 以降）。

> クラッシュからの再開は状態永続化（4.5 F-STATE-02）として、リソース上限の強制は Guard（4.4 F-GUARD-05）として既に要件化済みであり、本節では重複して定義しない。
