# Marmo-Core 要件定義書

> 本書は**叩き台**であり、レビューにより継続的に改訂される。

## 1. 概要

### 1.1 プロダクトビジョン
**Marmo-Core** は、OpenClaw に代表される AI エージェント基盤を参考とした、**汎用 AI Agent Kernel（カーネル）** である。
特定のユースケースやモデルベンダーに依存せず、あらゆる AI エージェントの「中核実行エンジン」として機能することを目指す。最終的には世界中の開発者・組織に採用される OSS / 基盤ソフトウェアとなることを目標とする。

### 1.2 「Kernel」という位置づけ
OS におけるカーネルが「プロセス・メモリ・I/O・権限」を管理するのと同様に、Marmo-Core は AI エージェントの実行に必要な以下を抽象化・管理する。

- **タスク（実行計画）の管理** … OS のプロセススケジューラに相当
- **リソース（Memory・Skill・Tool・Agent）の統一管理** … システムコール / デバイスドライバに相当
- **状態管理** … メモリ管理に相当
- **権限・安全性チェック** … 特権モード / アクセス制御に相当
- **外部環境との接続** … I/O サブシステムに相当
- **ログ・監査** … システムログ / 監査ログに相当

エージェントの「頭脳（LLM）」や「個性（プロンプト・ペルソナ）」は Kernel の外側（アプリケーション層）に置き、Marmo-Core はそれらを安全かつ確実に実行する**実行基盤レイヤ**に徹する。

### 1.3 設計原則
| 原則 | 内容 |
|------|------|
| モデル非依存 | 特定の LLM ベンダーに縛られない抽象化を提供する |
| 低依存・標準ライブラリ中心 | コアは標準ライブラリを優先しつつ、安全性・保守性・開発者体験のために必要な最小限の外部ライブラリを許容する。用途が限定的または重量級の依存は optional 依存として分離する |
| 安全第一 | あらゆる副作用を伴う操作は、権限・安全性チェックと監査の対象とする |
| 可観測性 | すべての判断・実行・状態遷移はログ化され、再現・追跡可能とする |
| 拡張性 | リソース（Memory・Skill・Tool・Agent）/ 外部接続はプラグインとして追加できる |
| 決定性と再現性 | 同一入力・同一状態からの実行は可能な限り再現可能とする |
| Human-in-the-loop | 重要操作には人間の確認を介在させられる |
| 統一リソースモデル | Memory・Skill・Tool・Agent を共通メタデータで管理し、保存形式と検索方式を共通化する。利用方法（inject / load / bind / delegate）は型ごとに分ける |

### 1.4 ポジショニング・差別化
LangGraph / AutoGen / CrewAI / OpenAI Agents SDK / Semantic Kernel など先行プロダクトが存在する。Marmo-Core が世界的に採用されるための差別化軸を以下に定める。

| 差別化軸 | 内容 | 競合に対する優位 |
|----------|------|------------------|
| 最小依存コア | コアは標準ライブラリを優先し、必要性を説明・監査できる少数の外部依存だけを採用する。依存ツリーを小さく保ち、監査・組込みを容易にする | 重い依存ツリーを持つ既存フレームワークに対し、企業・組込み・エッジで採用しやすい |
| 安全性ファースト | Guard 必須通過・監査ログ・HITL を**設計の中心**に据える（後付けでない） | 「とりあえず動く」系に対し、本番・規制業界での信頼を獲得 |
| Kernel 抽象 | OS カーネル的に実行・権限・状態・I/O を分離。LLM/プロンプトは外側 | モデル・ベンダーロックインを回避、長期保守性 |
| 統一リソースモデル | Memory・Skill・Tool・Agent を横断検索し、型別に活性化する設計 | ツール・エージェントが増加してもコンテキスト汚染なしにスケール |
| 再現性 | イベントソーシング＋リプレイで実行を完全再現 | デバッグ・監査・評価が容易 |

**ポジショニング・ステートメント（案）**:
> Marmo-Core は、必要最小限の依存で構成されたコアと「安全性を設計の中心に据えた」アーキテクチャを持つ、ベンダー非依存の AI Agent Kernel である。プロトタイプから規制業界の本番運用まで、同じ基盤でスケールする。

> 注: 本節は製品の位置づけを示す前文であり、実装要件ではない。上記の差別化軸・比較表は README とドキュメント冒頭に反映し、競合状況に応じて継続的に更新する。

### 1.5 対象ユーザーと Time-to-first-success
OSS 普及の最大の予測因子は「導入から最初の成功体験までの速さ」である。

**対象ユーザー（ペルソナ）**:
- エージェント開発者（個人 / スタートアップ）— 速く作りたい
- プラットフォーム / 基盤エンジニア — 自社基盤に組み込みたい
- 規制業界・セキュリティ重視の企業 — 監査可能で安全な基盤が要る

**DX 要件**:
- **F-DX-01**: `pip install marmo-core` が、必要最小限の外部依存だけで完了する。必須依存は、基盤の共通機能または標準の導入体験に必要な軽量ライブラリに限定し、目的・採用理由・ライセンス・保守状況を確認できるものとする。用途が限定的または重量級の依存は optional extra に分離する。
- **F-DX-02**: 30 秒以内・最小コード行数で動く「Hello World エージェント」を提供する（コピペで動作）。
- **F-DX-03**: デフォルト設定で安全に動作する（secure by default。明示許可なしに副作用を起こさない）。
- **F-DX-04**: エラー・拒否メッセージは原因と次の一手を示す教育的な内容とする。
- **F-DX-05**: 代表ユースケースの動くサンプル集（cookbook）を同梱・公開する。

---

## 2. スコープ

### 2.1 本フェーズで実装する中核コンポーネント
本要件定義書では、以下の 13 の中核コンポーネントを対象とする。

1. リソースレジストリ（Resource Registry）
2. 共通リトリーバー & セットセレクター（Retriever & Set Selector）
3. ポリシーゲートウェイ（Policy Gateway）
4. リソースアクティベータ（Resource Activator）
5. コンテキストコンパイラ（Context Compiler）
6. 実行計画（Planner）
7. ツール実行ランタイム（Tool Runtime）
8. エージェント実行ランタイム（Agent Runtime）
9. 状態管理（State Store）
10. 失敗時のリカバリ（Recovery Manager）
11. ログ・監査・実行評価（Audit Log & Execution Evaluator）
12. 人間確認（Human-in-the-loop / HITL Broker）
13. 外部環境との接続（Connector）

実装の着手順・段階分けは「8. 実装フェーズと優先度」を参照。

### 2.2 本フェーズでは対象外（将来検討）
- LLM そのものの学習・ファインチューニング
- 大規模 N 対 N マルチエージェントオーケストレーション（Agent 間の基本的な委譲は対象内。上位レイヤのオーケストレーション基盤は将来対応）
- GUI / 管理ダッシュボード（API・CLI を先行実装）
- 課金・マルチテナント基盤

---

## 3. アーキテクチャ概要

```
┌─────────────────────────────────────────────────────────────────┐
│                  Application Layer（エージェント実装）             │
│        （LLM 呼び出し・プロンプト・ペルソナ・UI）                 │
└─────────────────────────┬───────────────────────────────────────┘
                           │  Kernel API
┌─────────────────────────▼───────────────────────────────────────┐
│                       Marmo-Core Kernel                          │
│                                                                  │
│  ユーザー要求                                                     │
│      ↓                                                           │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Resource Registry（Memory・Skill・Tool・Agent 統一管理）  │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Retriever & Set Selector（4種類を横断検索・組み合わせ選択）│    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Policy Gateway（利用可否チェック → 実行許可チェック）      │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Resource Activator（型別活性化）                          │    │
│  │  memory→inject  skill→load  tool→bind  agent→delegate   │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                          ↓                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Context Compiler（LLM 入力構築・二段階 LLM 呼び出し）     │    │
│  └──────────────────────┬──────────────────────────────────┘    │
│                          ↓                                       │
│  ┌─────────────────────┐ ┌──────────────────────────────────┐   │
│  │ Planner（実行計画）  │→│ Tool Runtime / Agent Runtime     │   │
│  └─────────────────────┘ └──────────────────────────────────┘   │
│        │                              │                          │
│  ┌─────▼────────────────┐  ┌──────────▼───────────────────────┐ │
│  │ State Store / HITL   │  │ Recovery Manager / Audit Log      │ │
│  └──────────────────────┘  └──────────────────────────────────┘ │
└─────────────────────────┬───────────────────────────────────────┘
                           │  Connector API
┌─────────────────────────▼───────────────────────────────────────┐
│         外部環境（API / DB / ファイル / ブラウザ / OS / Agent）    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 機能要件

### 4.1 リソースレジストリ（Resource Registry）

#### 目的
エージェントが利用可能な4種類のリソース（Memory・Skill・Tool・Agent）を、統一スキーマで登録・記述・管理する。検索時は軽量なメタデータのみを使用し、完全な実体はアクティベーション時に取得することで、コンテキスト効率を最大化する。

#### 統一リソース種別
- **Memory**: 検索可能な知識・経験・コンテキスト情報。実行時にプロンプトに注入される。
- **Skill**: 手順・ルール・ワークフローを記述した高位の再利用可能な能力。指示としてロードされる。
- **Tool**: 単一の実行可能な関数・API 呼び出し。呼び出し可能な関数として動的に登録される。
- **Agent**: タスク委譲先となる専門エージェント。委譲インターフェース経由で接続される。

#### 共通メタデータ（全種別）
| フィールド | 説明 |
|-----------|------|
| id | 一意な識別子 |
| kind | リソース種別（memory / skill / tool / agent） |
| name | 名前 |
| version | バージョン（SemVer） |
| description | 自然言語による説明（検索・選択に利用） |
| capabilities | 提供する能力のリスト |
| input_summary | 入力の概要（検索用軽量表現） |
| output_summary | 出力の概要（検索用軽量表現） |
| required_permissions | 必要権限 |
| cost_estimate | 推定コスト（トークン / API コール / 時間） |
| latency_class | レイテンシ分類（fast / medium / slow） |
| side_effect | 副作用種別（none / read / write / external / irreversible） |
| trust_level | 信頼度（core / verified / community / untrusted） |
| ref | 実体を取得する参照先（URL / path / registry ID） |
| tags | タグ・カテゴリ |

#### 要件
- **F-REG-01**: 4種類のリソースを統一スキーマ（共通メタデータ）で登録・管理できる。
- **F-REG-02**: 検索時は共通メタデータ（軽量）のみを使用し、完全な定義（入力スキーマ詳細・実装・認証情報等）はアクティベーション時に取得する。
- **F-REG-03**: リソースはプラグインとして動的に追加・無効化できる（ホットロード対応を望ましい要件とする）。
- **F-REG-04**: バージョン管理を行い、互換性のないバージョンを区別できる。
- **F-REG-05**: 各リソースの実績メタデータ（成功率・平均実行時間・平均コスト・最終利用日時）を蓄積し、検索・選択・監査に利用できる。
- **F-REG-06**: Tool 固有の追加メタデータ（完全な入力スキーマ / JSON Schema 等）は、共通メタデータとは別に保持し、アクティベーション時のみ取得される。
- **F-REG-07**: Agent 固有の追加メタデータ（Agent Card: 能力詳細・入出力仕様・委譲インターフェース・対応タスク種別）を保持する。
- **F-REG-08**: Memory 固有の追加メタデータ（内容の種別・有効期限・ソース・埋め込みベクトルへの参照）を保持する。

---

### 4.2 共通リトリーバー & セットセレクター（Retriever & Set Selector）

#### 目的
タスクや状況に応じて、4種類のリソースを横断検索し、必要な組み合わせを選択する。単純な類似度ランキングではなく、コスト・権限・依存関係・信頼性を考慮した**集合選択**を行う。

#### 要件
- **F-RETR-01**: 自然言語のタスク記述から、Memory・Skill・Tool・Agent を横断検索できる。
- **F-RETR-02**: 検索方式として以下をサポートする。
  - キーワード / タグによるフィルタ（MVP は標準ライブラリ `difflib` 等による語句類似で実現）
  - 説明文に対するセマンティック検索（埋め込みは差し替え可能なインターフェースとし、外部依存を要する実装は optional 拡張に分離する）
- **F-RETR-03**: 検索結果を以下の複合スコアでランキングできる。
  - タスクとの関連度
  - 種別（kind）の適合度
  - コスト効率
  - 利用可能な権限との適合
  - 依存関係の解決可能性
  - 信頼度・過去の成功率
- **F-RETR-04**: 単一リソースではなく、**必要な組み合わせ（Set）** を選択できる（Set Selector）。例: タスク X には memory A + skill B + tool C + agent D の組み合わせが最適。
- **F-RETR-05**: 大量のリソースが登録されても、LLM のコンテキストに渡す候補数を制御できる（Top-K 制限、種別ごとの上限設定）。
- **F-RETR-06**: コンテキスト（現在の状態・利用可能な権限・過去の成功率）を考慮して候補を絞り込める。
- **F-RETR-07**: 選択結果・選択理由・スコアを監査ログに残せる（監査・改善のため）。

---

### 4.3 ポリシーゲートウェイ（Policy Gateway）

#### 目的
副作用を伴う操作を実行前に検査し、許可されていない・危険な操作を防止する。
**「検索ヒット ≠ 利用可能 ≠ 実行許可」** の三段階を明示的に分離することで、意図しないリソースの活性化・実行を防ぐ。

#### 三段階チェックモデル
1. **アクティベーションゲート（利用可否チェック）**: 検索ヒット後、リソースを活性化してよいかを確認する。
2. **実行ゲート（実行許可チェック）**: 活性化済みリソースを実際に実行してよいかを確認する。
3. **結果ゲート（出力検査）**: 実行結果を外部へ渡す前に安全性を確認する（任意）。

#### 要件
- **F-GATE-01**: すべてのリソースのアクティベーションと実行は Policy Gateway を通過する（バイパス経路を設けない）。
- **F-GATE-02**: 権限モデルを定義する。
  - リソースごとに required_permissions を宣言（4.1 の共通メタデータ）。
  - 実行コンテキストに付与された granted_permissions と突き合わせる。
- **F-GATE-03**: ポリシーエンジンにより、許可 / 拒否 / 人間確認要求（escalate）の判定を行える。ポリシーは宣言的に記述・差し替え可能とする。
- **F-GATE-04**: 確認する項目は以下を含む。
  - ユーザー / コンテキストに当該リソースへのアクセス権限があるか
  - 認証できるか（trust_level の確認）
  - コスト上限内か
  - 書き込み・削除などの副作用（side_effect）があるか
  - 信頼できる提供元か（trust_level）
  - 入出力形式が互換か
- **F-GATE-05**: 副作用の種別（none / read / write / external / irreversible）に応じた段階的な制御を行う。不可逆・外部送信操作はデフォルトで人間確認を要求できる。
- **F-GATE-06**: リソース制限（実行時間・呼び出し回数・コスト上限・レート制限）を設定・強制できる。
- **F-GATE-07**: 危険操作（破壊的コマンド・機密情報の外部送信など）の検出ルールを持ち、ブロックまたは escalate する。
- **F-GATE-08**: ドライラン（実際の副作用を起こさず計画・検証のみ行う）モードをサポートする。
- **F-GATE-09**: すべての判定結果（許可 / 拒否 / escalate と理由）は監査ログに記録される。

---

### 4.4 リソースアクティベータ（Resource Activator）

#### 目的
Policy Gateway の承認後、リソースを種別ごとのパターンで実体化する。**保存形式と検索方式は共通化し、利用方法は型ごとに分ける**ことで、一貫したインターフェースと最適な実行を両立する。

#### 型別アクティベーションパターン
```
memory → retrieve and inject  （必要部分をコンテキストに注入）
skill  → retrieve and load    （手順・制約として指示にロード）
tool   → retrieve and bind    （呼び出し可能関数として動的登録）
agent  → retrieve and delegate（委譲先として接続）
```

#### 要件
- **F-ACT-01**: Memory のアクティベーション。
  - 検索された Memory の本文を、必要な範囲だけ取得する（全文ではなく関連部分のみ）。
  - コンテキストに注入する形式（プロンプトへの埋め込み）に変換する。
- **F-ACT-02**: Skill のアクティベーション。
  - 手順・ルール・ワークフローを指示テキストとしてロードする。
  - 制約・注意事項を Planner への入力として追加する。
- **F-ACT-03**: Tool のアクティベーション。
  - アクティベーション時に完全な入力スキーマ（詳細 JSON Schema）を取得する。
  - そのツールだけを LLM の利用可能ツール一覧に動的に追加する（全ツールを一括渡しにしない）。
  - ツール本体のコード・認証情報・シークレットは LLM には渡さず、実行基盤（Tool Runtime）側で管理する。
- **F-ACT-04**: Agent のアクティベーション。
  - Agent Card（能力詳細・入出力仕様・対応タスク種別・必要権限）を取得する。
  - 委譲インターフェース（Tool ラップ型 / 構造化タスク型）を選択・準備する。
- **F-ACT-05**: アクティベーション結果（種別・取得した情報・所要時間）を監査ログに記録する。
- **F-ACT-06**: アクティベーション失敗時（参照先への接続エラー・スキーマ不整合等）はリカバリ（4.10）へ通知する。

---

### 4.5 コンテキストコンパイラ（Context Compiler）

#### 目的
アクティベーション済みリソース（Memory・Skill・Tool・Agent）を LLM への入力に変換する。**二段階 LLM 呼び出しパターン**をサポートすることで、大量のリソース定義を毎回渡すことなくコンテキストを効率化する。

#### 二段階 LLM 呼び出しパターン
```
1回目（リソース選択フェーズ）:
  入力: ユーザー要求 + リソース候補の軽量メタデータ一覧
  出力: 必要なリソースの選択（kind + id の組み合わせ）

2回目（実行フェーズ）:
  入力: ユーザー要求
      + 注入された Memory テキスト
      + ロードされた Skill 指示
      + 選択された Tool の詳細スキーマ
      + Agent 委譲インターフェース
  出力: 実際のタスク処理
```

#### 要件
- **F-CTX-01**: アクティベーション済みの Memory・Skill・Tool・Agent を受け取り、LLM 入力（プロンプト + ツール定義 + システム指示）を構築する。
- **F-CTX-02**: 二段階 LLM 呼び出しパターンをサポートする。1回目で軽量メタデータから必要なリソースを選択し、2回目で選択済みリソースのみを渡す。
- **F-CTX-03**: Memory テキストはコンテキスト長を考慮し、重要度・関連度でトリミングできる。
- **F-CTX-04**: Tool スキーマは LLM のツールコール形式（OpenAI / Anthropic 等）に変換できる（4.14 LLM アダプタと連携）。
- **F-CTX-05**: Agent 委譲インターフェースは、LLM から呼び出し可能な形式（Tool ラップまたは構造化タスク形式）で提示する。
- **F-CTX-06**: コンテキスト構築の結果（使用リソース一覧・推定トークン数・構成）を監査ログに記録する。
- **F-CTX-07**: コンテキスト長の上限に対するフィッティング（優先度ベースのトリミング）を設定できる。

---

### 4.6 実行計画（Planner）

#### 目的
与えられた目標（ゴール）を、活性化済みリソースを前提として実行可能なステップ列・依存グラフへ分解する。

#### 要件
- **F-PLAN-01**: 目標から実行計画（Plan）を生成できる。Plan は順序付きステップまたは DAG（有向非巡回グラフ）として表現する。
- **F-PLAN-02**: 各ステップは、使用するリソース（種別・ID）、入力、期待される出力、依存関係を持つ。
- **F-PLAN-03**: Plan は実行前に検証される（未解決の依存・未許可の権限・存在しないリソースを検出）。
- **F-PLAN-04**: 実行中の結果に応じて Plan を再計画（re-plan）できる。
- **F-PLAN-05**: 並列実行可能なステップを識別し、依存関係を満たす範囲で並列化できる（望ましい要件。実現方式は「5. 実装方針」の並行モデルに従う）。
- **F-PLAN-06**: Plan は永続化され、中断・再開が可能（状態管理と連携）。
- **F-PLAN-07**: 計画方式は差し替え可能とする（LLM ベース / ルールベース / ハイブリッド）。

---

### 4.7 ツール実行ランタイム（Tool Runtime）

#### 目的
Policy Gateway の実行許可を得た Tool を、安全かつ確実に実行する。認証情報・シークレットは LLM に渡さず、実行基盤側で管理する。

#### 要件
- **F-TOOL-01**: アクティベーション済み Tool を受け取り、実行する。Tool 本体のコード・認証情報は実行基盤側で保持し、LLM には渡さない。
- **F-TOOL-02**: 実行前に入力値を Tool の詳細スキーマで検証する（型・必須フィールド・値域）。
- **F-TOOL-03**: タイムアウト・リソース制限（メモリ・CPU）を適用できる。
- **F-TOOL-04**: 実行結果（出力・所要時間・エラー）を返却し、State Store と監査ログに記録する。
- **F-TOOL-05**: 実行失敗時はリカバリ（4.10）へ通知する。
- **F-TOOL-06**: Connector（4.13）と連携して外部環境へアクセスする。

---

### 4.8 エージェント実行ランタイム（Agent Runtime）

#### 目的
Policy Gateway の承認を得た Agent へタスクを委譲する。委譲方式は処理の複雑さに応じて選択できる。

#### 委譲方式
```
方式1（ラップ委譲）: 単純処理・短時間処理
  main agent → delegate_review(...) → specialist agent
  AgentをToolとしてラップし、同期的に呼び出す。

方式2（構造化タスク委譲）: 複雑処理・長時間処理・状態管理が必要な処理
  main agent → 構造化タスク → specialist agent
  タスクを構造化して委譲し、Agent は独立した実行コンテキストで処理する。
```

#### 構造化タスクの要素
- 目的（goal）
- 入力（input）
- 制約（constraints）
- 利用可能ツール（available_tools）
- 予算（budget）
- 期限（deadline）
- 期待する出力形式（output_format）
- 成功条件（success_criteria）

#### 要件
- **F-AGENT-01**: 単純処理は Agent を Tool としてラップし、Tool Runtime 経由で同期呼び出しできる。
- **F-AGENT-02**: 長時間処理・状態管理が必要な処理は、構造化タスクとして委譲できる。
- **F-AGENT-03**: 委譲先 Agent へ渡す権限は、委譲元の権限の部分集合に制限する（権限の昇格を許可しない）。
- **F-AGENT-04**: 委譲先 Agent は独立した実行コンテキスト（独自の State Store・Audit Log スコープ）で動作できる。
- **F-AGENT-05**: 委譲結果（成功 / 失敗 / タイムアウト・出力・所要時間・コスト）を受け取り、State Store と監査ログに記録する。
- **F-AGENT-06**: 委譲失敗時はリカバリ（4.10）へ通知する。
- **F-AGENT-07**: 委譲の深さ（ネスト数）・コスト合計に上限を設定し、無限委譲ループを防止する。

---

### 4.9 状態管理（State Store）

#### 目的
エージェントの実行状態・コンテキスト・成果物を一貫して保持し、中断・再開・再現を可能にする。

> **注意**: State Store は「実行状態」を管理する。「検索・注入される知識」は Resource Registry の Memory リソースが担い、両者は明確に区別する。

#### 要件
- **F-STATE-01**: セッション / タスク単位で状態を保持する。状態には Plan・各ステップの実行結果・変数・アクティベーション済みリソース情報を含む。
- **F-STATE-02**: 状態は永続化でき、プロセス再起動後も再開できる。
- **F-STATE-03**: 状態遷移はイベントとして記録され、任意時点の状態を再構築できる（イベントソーシング的アプローチを推奨）。
- **F-STATE-04**: チェックポイントを作成し、特定時点へロールバックできる（失敗時リカバリと連携）。
- **F-STATE-05**: ストレージバックエンドは差し替え可能とする（インメモリ / ファイル / RDB / KVS。MVP は標準ライブラリの `json` ファイルまたは同梱の `sqlite3` を採用し、外部 DB は拡張とする）。
- **F-STATE-06**: 短期記憶（実行中コンテキスト）と長期記憶（セッションを跨ぐ知識）を区別して扱える。長期記憶は Resource Registry の Memory リソースとして管理することを推奨する。
- **F-STATE-07**: 機密データは原則として状態に保存しない。必要な場合は `SecretRef` 等の参照として扱い、ログ・LLM プロンプト・永続状態へ平文を混入させない。
- **F-STATE-08**: 並行アクセスに対する一貫性（楽観ロック / バージョニング）を保証する。

---

### 4.10 失敗時のリカバリ（Recovery Manager）

#### 目的
ステップ実行の失敗から、自動またはガイド付きで回復する。リソースのアクティベーション失敗・Tool 実行失敗・Agent 委譲失敗のいずれも扱う。

#### 要件
- **F-RECOV-01**: 失敗を分類する（一時的エラー / 永続的エラー / タイムアウト / 権限拒否 / 検証失敗 / アクティベーション失敗 / 委譲失敗）。
- **F-RECOV-02**: リトライ戦略を設定できる（最大回数、指数バックオフ、ジッタ）。リトライ可否は失敗分類に基づく。
- **F-RECOV-03**: 失敗時に代替リソース（Tool / Agent）へフォールバックできる（Retriever に再検索を依頼）。
- **F-RECOV-04**: チェックポイントへロールバックし、再計画（4.6 F-PLAN-04）して再実行できる。
- **F-RECOV-05**: 補償処理（compensation / saga パターン）を定義し、既に発生した副作用を取り消せる（取り消し可能な操作について）。
- **F-RECOV-06**: 自動回復不能と判断した場合は、人間確認（4.12）へエスカレーションする。
- **F-RECOV-07**: リカバリの全過程（失敗内容・採用した戦略・結果）を監査ログに記録する。
- **F-RECOV-08**: 無限ループ・暴走を防ぐサーキットブレーカー（連続失敗時の停止）を備える。

---

### 4.11 ログ・監査・実行評価（Audit Log & Execution Evaluator）

#### 目的
すべての判断・実行・状態遷移・人間介入を記録し、再現性・説明可能性・コンプライアンスを担保する。加えて、成功率・コスト・レイテンシを集計し、リソース選択の改善に役立てる。

#### 要件
- **F-LOG-01**: 構造化ログ（機械可読、例: JSON）で出力する（MVP は標準 `logging` + JSON フォーマッタで実現）。
- **F-LOG-02**: 記録対象は少なくとも以下を含む。
  - リソース検索と選択理由（Retriever / Set Selector の判断）
  - Policy Gateway の判定（許可 / 拒否 / escalate と理由）
  - リソースアクティベーション（種別・取得内容・所要時間）
  - Context Compiler の構成（使用リソース・推定トークン数）
  - 実行計画の生成と再計画
  - Tool / Agent の実行（入力・出力・所要時間・結果）
  - 状態遷移・チェックポイント
  - 失敗とリカバリ
  - 人間確認の要求・応答
  - 外部環境との通信
- **F-LOG-03**: 各レコードは一意の相関 ID（trace / span）でひも付けられ、1 タスクの全フローを追跡できる。
- **F-LOG-04**: 監査ログは改竄検知可能とする（追記専用 / ハッシュチェーンを望ましい要件とする。`hashlib` で実現可能）。
- **F-LOG-05**: 機密情報（資格情報・個人情報）はマスキング / 編集して記録する。
- **F-LOG-06**: ログレベル・出力先（標準出力 / ファイル / 外部監視基盤）を設定できる。
- **F-LOG-07**: ログから実行をリプレイ・検証できる（再現性、4.9 F-STATE-03 と連携）。
- **F-LOG-08**: **Execution Evaluator**: リソースごとに成功率・平均レイテンシ・平均コストを集計し、Resource Registry のメタデータ（F-REG-05）にフィードバックする。これにより Retriever のスコアリングを継続改善できる。
- **F-LOG-09**: メトリクスのエクスポート（OpenTelemetry 等への対応は optional 拡張とする）。

---

### 4.12 人間確認（Human-in-the-loop / HITL Broker）

#### 目的
重要・危険・不確実な操作について、人間の承認・入力・判断を介在させる。

#### 要件
- **F-HITL-01**: Policy Gateway やリカバリからのエスカレーションを受け、人間への確認要求を生成できる。
- **F-HITL-02**: 確認要求には、対象操作・想定される影響・代替案・推奨判断を含める。
- **F-HITL-03**: 応答種別として、承認 / 拒否 / 修正（入力差し替え） / 保留 をサポートする。
- **F-HITL-04**: 確認待ちの間、対象タスクは状態を保存して安全に一時停止し、応答後に再開できる（4.9 と連携）。
- **F-HITL-05**: 確認の通知・応答チャネルは差し替え可能とする（CLI / API / Webhook / チャット等）。
- **F-HITL-06**: タイムアウト時のデフォルト挙動（拒否 / 保留継続）を設定できる。
- **F-HITL-07**: 承認ポリシーを設定できる（特定操作は常に確認、特定ユーザのみ承認可、など）。
- **F-HITL-08**: すべての確認要求と応答（誰が・いつ・何を）を監査ログに記録する。

---

### 4.13 外部環境との接続（Connector）

#### 目的
ファイル・API・DB・ブラウザ・OS など外部環境への接続を、安全かつ統一的に抽象化する。

#### 要件
- **F-CONN-01**: 外部接続を Connector として抽象化し、統一インターフェースで扱う。
- **F-CONN-02**: 標準的な Connector 種別を想定する（HTTP/REST API、ファイルシステム、シェル / OS コマンド、データベース、ブラウザ自動化、メッセージング）。MVP は標準ライブラリのみで実現できる種別（`urllib`=HTTP、ファイル、`subprocess`=シェル、`sqlite3`=DB）を優先する。
- **F-CONN-03**: Connector はプラグインとして追加できる（4.1 のリソースレジストリと整合）。
- **F-CONN-04**: 外部接続は必ず Policy Gateway（4.3）の権限・安全性チェックを経る。
- **F-CONN-05**: 資格情報（API キー・トークン）は秘匿管理し、ログに平文で出さない（シークレットストア連携を想定）。
- **F-CONN-06**: タイムアウト・リトライ・レート制限・サーキットブレーカーを Connector レベルで設定できる。リトライ / サーキットブレーカーの戦略はリカバリ（4.10）と、リソース / コスト上限は Policy Gateway（4.3 F-GATE-06）と整合させる。
- **F-CONN-07**: サンドボックス / 隔離環境での実行をサポートする（特にシェル・ブラウザなど高リスク接続）。隔離レベル（L0〜L3）の定義・宣言・検査はセキュリティ節（4.16 F-SEC-05）を正本とする。
- **F-CONN-08**: 外部とのすべての送受信は監査ログに記録される（機密はマスキング）。
- **F-CONN-09**: 標準プロトコル（例: MCP = Model Context Protocol）との相互運用を将来的にサポートできる拡張余地を持つ。

---

### 4.14 LLM アダプタ層（LLM Provider）

#### 目的
LLM 本体はカーネル外（アプリ層）に置くという原則を保ちつつ、ユーザーが最短で LLM に接続できる**標準アダプタ interface と参照実装**を提供する。Context Compiler（4.5）と連携し、二段階 LLM 呼び出しパターンをサポートする。

#### 要件
- **F-LLM-01**: LLM 接続を抽象 interface（`LLMProvider`）として定義する。コアはこの interface のみに依存し、具象実装は持たない。
- **F-LLM-02**: 参照実装として、API 経由の LLM（OpenAI / Anthropic 等）とローカル LLM の両方を **optional extra** として提供する。未導入でも、コアとその最小必須依存だけで基本機能が動作する。
- **F-LLM-03**: interface は少なくとも、チャット補完・ツールコール・ストリーミング・トークン使用量の返却を抽象化する。
- **F-LLM-04**: テスト・リプレイ用のモック / 決定論的 LLM 実装を、**追加の LLM SDK や外部サービスを必要とせず同梱**する（F-DX・再現性と連携）。モック LLM は二段階呼び出しパターンにも対応する。
- **F-LLM-05**: ツールコール形式（OpenAI / Anthropic 等）と Resource Registry のメタデータ（4.1）を相互変換できる。
- **F-LLM-06**: トークン使用量・コストを監査ログ（F-LOG-09）・Policy Gateway のコスト上限（F-GATE-06）へ受け渡す。
- **F-LLM-07**: アプリケーション利用者は、プロバイダーごとの差分（API 仕様・ツールコール形式・ストリーミング形式・エラー形式）を原則として意識せず、統一された `LLMProvider` interface を通じて利用できる。
- **F-LLM-08**: 二段階 LLM 呼び出しパターン（Context Compiler 4.5 と連携）をサポートする。1回目の呼び出し（リソース選択）と2回目の呼び出し（実行）を統一 interface で扱える。

---

### 4.15 リソースの配布・パッケージング（Distribution / Registry）

#### 目的
カーネルのインパクトは**プラグイン・エコシステムの大きさ**で決まる。4種類のリソース（Memory・Skill・Tool・Agent）を共有・配布・再利用するための規約を定める。

#### 要件
- **F-DIST-01**: リソースパッケージ形式を定義する（共通メタデータ + 実体 + 依存宣言 + 種別固有メタデータ）。4種類すべてに対応する。
- **F-DIST-02**: 名前空間とバージョニング（SemVer）を持ち、互換性を判定できる。
- **F-DIST-03**: 検出・取得元として、ローカルディレクトリを最小実装でサポートする。Git リポジトリからの取得と集中レジストリは v2 以降の拡張として扱う。
- **F-DIST-04**: パッケージの改竄検知として、MVP ではハッシュ固定・ロックファイル・取得元記録を提供する。暗号学的な署名・検証は optional 拡張として扱う。
- **F-DIST-05**: 各パッケージは対応するカーネルバージョン範囲を宣言し、非互換を導入前に検出する。
- **F-DIST-06**: MCP サーバー（F-CONN-09）をリソース供給源として取り込むアダプタを将来サポートする。
- **F-DIST-07**: 公開・共有を促すドキュメント（リソース作成ガイド・テンプレート）を整備する。

---

### 4.16 セキュリティと脅威モデル

#### 目的
AI エージェントは外部の信頼できない内容（Web・ファイル・ツール出力・Agent の出力）を読み、それに基づいて副作用のある操作を行う。**プロンプトインジェクション**（注入された指示による意図しない操作・情報漏洩）はエージェント最大のリスクであり、カーネルの設計中心に据える。Policy Gateway（4.3）が「権限の検査」を担うのに対し、本節は「信頼できない内容由来の指示」への対処を担う。

#### 想定脅威（脅威モデル）
- プロンプトインジェクション（直接 / 間接：取得したコンテンツ・Memory・Agent 出力に埋め込まれた指示）
- 機密情報の外部送信（データ持ち出し / exfiltration）
- 悪意ある Skill / Tool / Agent / Memory パッケージ（供給網）
- 過剰権限・権限昇格（Agent 委譲による権限昇格を含む）
- 破壊的・不可逆操作の誘発
- 資格情報・秘密情報の漏洩

#### 要件
- **F-SEC-01**: データの出所を信頼境界でラベリングする（信頼できる指示 / 信頼できない内容）。ツール出力・外部取得コンテンツ・Memory 本文・Agent 出力はデフォルトで「信頼できない」として扱う。
- **F-SEC-02**: 信頼できない内容に含まれる「指示らしき文字列」を、実行すべき指示として自動昇格させない（content と instruction の分離）。
- **F-SEC-03**: 副作用操作は宣言された Plan / 活性化済みリソースの範囲に限定し、信頼できない内容が新たな副作用操作を直接誘発できないようにする。
- **F-SEC-04**: データ持ち出し防止。外部送信先のアロー/ブロックリスト、機密データを外部送信操作へ渡す際の検出と escalate（4.3 / 4.12 連携）。
- **F-SEC-05**: 実行隔離（サンドボックス）の保証レベルを定義し、Connector ごとに宣言する（F-CONN-07 を具体化）。
  - L0: 隔離なし（読み取り専用・低リスクのみ）
  - L1: プロセス分離・リソース制限（`subprocess` + タイムアウト / リソース上限）
  - L2: ファイルシステム / ネットワークの制限（隔離ディレクトリ・許可ホストのみ）
  - L3: コンテナ / VM 等の強隔離（高リスク：シェル・ブラウザ。外部依存可の optional 拡張）
- **F-SEC-06**: 秘密情報（資格情報・トークン）は `SecretRef` として参照し、状態・ログ・LLM プロンプトへ平文で混入させない（4.9 F-STATE-07 / 4.11 F-LOG-05 と整合）。
- **F-SEC-07**: 最小権限をデフォルトとし、リソースは宣言した required_permissions の範囲でのみ動作する。
- **F-SEC-08**: **Agent 信頼モデル**: Agent 委譲時、委譲先 Agent の trust_level を検証する。untrusted な Agent への委譲は Policy Gateway でブロックまたは escalate する。委譲先 Agent は委譲元の権限の部分集合のみ取得できる（権限昇格の禁止）。
- **F-SEC-09**: 脅威モデルと対策を `docs/threat-model` に文書化し、リリースごとに見直す。
- **F-SEC-10**: インジェクション・データ持ち出しの試行を検出した場合は監査ログに記録し、必要に応じて人間確認へ escalate する。

> 注: カーネルはプロンプトインジェクションを完全に「防止」できないことを前提に、**被害を局所化する**（最小権限・隔離・人間確認・監査）多層防御の立場をとる。

---

## 5. 実装方針（Python / 低依存）

### 5.1 言語・ランタイム
- **言語**: Python。型ヒント・`dataclasses`・`match` 文・`asyncio` の TaskGroup を活用するため、サポート下限を **Python 3.11** とする（要レビュー）。
- **コアの外部依存**: 標準ライブラリを第一選択とするが、安全性・保守性・相互運用性・開発者体験を明確に改善する場合は、必要最小限の外部依存を許容する。必須依存は軽量で、基盤の共通機能または標準の導入体験に必要であり、保守状況・互換バージョン範囲・ライセンスを確認できるものに限定する。
- **依存管理**: 新しい必須依存の追加は、標準ライブラリまたは小規模な自前実装では不十分な理由、依存ツリーへの影響、セキュリティ・ライセンス・保守状況をレビューする。使用しなくなった依存は削除し、直接依存と推移的依存を継続的に監査する。
- **拡張の依存**: セマンティック検索・外部 DB・ブラウザ自動化・OpenTelemetry 等、用途が限定的または重量級の外部ライブラリは optional extra（例: `marmo-core[semantic]`）として本体から分離する。

### 5.2 標準ライブラリへのマッピング（MVP の最小依存実装方針）
最小依存は「Core の依存ツリーを小さく、説明可能かつ監査可能に保つ」ための方針である。標準ライブラリを優先する一方、十分に検証された外部ライブラリの採用が安全性・保守性を高める場合は利用を許容する。暗号化・署名・強隔離のような安全保証を、依存数の削減だけを目的として無理に自前実装しない。

| 要件 | 一般的な外部依存 | MVP（標準ライブラリ優先・最小依存）方針 |
|------|----------------|-----------------------------|
| リソースセマンティック検索 (F-RETR-02) | numpy / FAISS / 埋め込み SDK | まず `difflib` による語句類似。ベクトル検索はインターフェースのみ定義し後付け |
| 状態永続化 (F-STATE-05) | SQLAlchemy / Redis | `json` + ファイル、または同梱の `sqlite3` |
| 構造化ログ (F-LOG-01) | structlog / loguru | 標準 `logging` + JSON フォーマッタ |
| 改竄検知 (F-LOG-04) | 専用ライブラリ | `hashlib` によるハッシュチェーン |
| メトリクス (F-LOG-09) | OpenTelemetry SDK | インターフェースのみ定義、実装は optional 拡張 |
| HTTP 接続 (F-CONN-02) | requests / httpx | `urllib.request` |
| シェル接続 (F-CONN-02) | — | `subprocess` |
| 入出力スキーマ検証 (F-REG-06) | jsonschema / pydantic | `dataclasses` + 自前バリデータ（軽量 JSON Schema サブセット） |
| 機密情報管理 (F-STATE-07 / F-SEC-06) | keyring / Vault SDK | Core は `SecretRef` とマスキングのみ。暗号化保存・外部 Secret Store は optional 拡張 |
| パッケージ署名 (F-DIST-04) | sigstore / GPG | MVP はハッシュ固定・ロックファイル・取得元記録。署名検証は optional 拡張 |
| 強隔離 (F-CONN-07 / F-SEC-05) | Docker / Firecracker | Core は隔離レベル宣言・Policy Gateway 検査・監査のみ。L3 実行は optional 拡張 |
| コンテキスト長管理 (F-CTX-07) | tiktoken / transformers | インターフェースのみ定義。実装は LLM アダプタ optional 拡張 |

### 5.3 並行モデル
- 外部接続待ち（I/O バウンド）が処理の大半を占めるため、**`asyncio` を中核**に据える。標準同梱であり追加依存が不要。
- 並列実行可能なステップ（F-PLAN-05）は依存解決後に `asyncio.gather` / `TaskGroup` で並行実行する。
- ブロッキング処理・CPU バウンド処理（一部 Connector）は `loop.run_in_executor`（`ThreadPoolExecutor`）へ退避する。
- 同期 API も提供し、`asyncio.run` でラップして同期利用できるようにする。

### 5.4 拡張インターフェースの方針
- 各エンジン（Planner / Retriever / SetSelector / PolicyGateway / ResourceActivator / ContextCompiler / ToolRuntime / AgentRuntime / StateStore / Recovery / AuditLog / HITLBroker / Connector）は `abc.ABC` による抽象基底クラスで定義し、差し替え可能とする。
- カーネルは具象実装を直接 import せず、登録（依存性注入）で受け取る。

---

## 6. Kernel 公開 API（概念設計）

アプリケーション層は以下の最小 API でカーネルを駆動する（シグネチャは概念であり確定ではない）。

```python
class Kernel:
    # リソース登録（プラグイン注入）
    def register_resource(self, metadata: ResourceMetadata, impl: Any) -> None: ...
    def register_connector(self, name: str, connector: Connector) -> None: ...
    def set_policy(self, policy: Policy) -> None: ...
    def set_planner(self, planner: Planner) -> None: ...
    def set_retriever(self, retriever: Retriever) -> None: ...

    # 実行ライフサイクル
    async def submit(self, goal: Goal, context: ExecutionContext) -> TaskId: ...
    async def run(self, task_id: TaskId) -> TaskResult: ...      # 完了 or 一時停止まで進める
    async def resume(self, task_id: TaskId,
                     hitl_response: HitlResponse | None = None) -> TaskResult: ...
    async def cancel(self, task_id: TaskId) -> None: ...

    # 観測
    def get_state(self, task_id: TaskId) -> TaskState: ...
    def subscribe(self, handler: Callable[[Event], None]) -> None: ...  # ログ/進捗イベント
```

`run` はゴール完了・失敗・**人間確認待ちでの一時停止**のいずれかで返る。一時停止時はアプリ層が応答を集め、`resume` で再開する（F-HITL-04 / F-STATE-02）。

---

## 7. データモデル例（概念）

確定スキーマではなく、各コンポーネント間で受け渡す主要構造の骨子。

```python
@dataclass
class ResourceMetadata:                    # 4.1 共通メタデータ（全リソース種別）
    id: str
    kind: Literal["memory", "skill", "tool", "agent"]
    name: str
    version: str
    description: str
    capabilities: list[str]
    input_summary: str
    output_summary: str
    required_permissions: list[str]
    cost_estimate: float
    latency_class: Literal["fast", "medium", "slow"]
    side_effect: Literal["none", "read", "write", "external", "irreversible"]
    trust_level: Literal["core", "verified", "community", "untrusted"]
    ref: str                               # 実体取得先
    tags: list[str]

@dataclass
class ToolExtended:                        # 4.1 Tool 固有拡張（アクティベーション時に取得）
    metadata: ResourceMetadata
    input_schema: dict                     # 完全な JSON Schema
    output_schema: dict

@dataclass
class AgentCard:                           # 4.1 Agent 固有拡張（アクティベーション時に取得）
    metadata: ResourceMetadata
    capabilities_detail: list[str]
    delegation_interface: Literal["tool_wrap", "structured_task"]
    supported_task_kinds: list[str]

@dataclass
class MemoryChunk:                         # 4.1 Memory 固有（アクティベーション時に取得）
    metadata: ResourceMetadata
    content: str                           # 注入されるテキスト
    relevance_score: float

@dataclass
class ResourceSet:                         # 4.2 Set Selector の選択結果
    memories: list[MemoryChunk]
    skills: list[ResourceMetadata]
    tools: list[ToolExtended]
    agents: list[AgentCard]
    selection_reason: str

@dataclass
class Step:                                # 4.6
    id: str
    resource_id: str
    resource_kind: Literal["memory", "skill", "tool", "agent"]
    inputs: dict
    depends_on: list[str]
    status: Literal["pending", "running", "done", "failed", "skipped"]

@dataclass
class Plan:                                # 4.6
    goal: str
    steps: list[Step]

@dataclass
class PolicyDecision:                      # 4.3
    verdict: Literal["allow", "deny", "escalate"]
    reason: str
    gate: Literal["activation", "execution", "output"]

@dataclass
class AuditRecord:                         # 4.11
    trace_id: str
    span_id: str
    timestamp: str
    kind: str   # retrieve / policy / activate / compile / plan / execute / state / recovery / hitl / connector
    payload: dict                          # 機密はマスキング済み
    prev_hash: str | None                  # ハッシュチェーン（改竄検知）
    hash: str

@dataclass
class DelegationTask:                      # 4.8 構造化タスク委譲
    goal: str
    input: dict
    constraints: list[str]
    available_tools: list[str]
    budget: float | None
    deadline: str | None
    output_format: str
    success_criteria: list[str]
```

---

## 8. 実装フェーズと優先度

MoSCoW を併記する（M=Must / S=Should / C=Could）。各バージョンは**検証可能な価値**を区切りとする。v1 は Kernel の実行ループまで含めず、統一リソースモデルと統一検索を CLI で検証できる範囲に限定する。

### 8.1 v1 の境界
v1 は「4種類のリソースを同じ検索面で扱える」ことを確認するための最小リリースである。目的は、実行基盤を作り込む前に、Resource Registry と Retriever / Set Selector のデータモデル・検索品質・CLI 体験を固定することである。

v1 で固定する不変条件は以下とする。

- Memory・Skill・Tool・Agent を共通メタデータで表現できる。
- ローカル定義ファイルから4種類のリソースを読み込める。
- タスク文・キーワード・タグ・kind・trust_level・side_effect で横断検索できる。
- 検索結果にスコア、選択理由、メタデータ概要を表示できる。
- Top-K、kind 別上限、権限・信頼度・副作用によるフィルタを指定できる。
- CLI で validate / list / search / inspect を実行し、統一検索の妥当性を確認できる。
- サンプルリソースと検索シナリオにより、追加の optional 依存や外部サービスなしで挙動を再現できる。

v1 では以下を対象外とする。

- Policy Gateway の実行ゲート / 結果ゲート。
- Resource Activator による Memory inject / Skill load / Tool bind / Agent delegate。
- Context Compiler、Planner、Tool Runtime、Agent Runtime。
- State Store、Recovery Manager、HITL Broker、Connector。
- 実 LLM Provider、モック LLM による Hello World 実行。
- リソース配布、Git 取得、集中レジストリ、署名検証。
- 監査ログのハッシュチェーン、OpenTelemetry、サービス / デーモン運用、水平スケール。

### 8.2 バージョン計画

| Version | 含む要件 | 優先度 | ゴール |
|---------|---------|--------|--------|
| **v1 — Unified Search CLI** | 4種類リソースの共通メタデータ(4.1 最小)、ローカル定義の読み込み、キーワード / タグ / kind 横断検索(4.2 最小)、Top-K / kind 別上限 / trust_level / side_effect フィルタ、最小 Set Selector(4.2 F-RETR-04 のルールベース版)、検索理由とスコア表示、CLI validate / list / search / inspect、サンプルリソース | M | Memory・Skill・Tool・Agent を統一スキーマで登録し、CLI から横断検索・候補選択を検証できる。実行・活性化・LLM 呼び出しは行わない |
| **v2 — Executable Kernel** | Policy Gateway 三段階(4.3)、Resource Activator(4.4)、Context Compiler(4.5)、線形 Planner(4.6 最小)、Tool Runtime(4.7 最小)、Agent Runtime のラップ委譲(4.8 基本)、State Store(4.9 基本)、Recovery(4.10 基本)、構造化 Audit Log(4.11 基本)、HITL Broker(4.12 基本)、基本 Connector(4.13: HTTP/ファイル/シェル/sqlite3)、LLMProvider interface とモック LLM(4.14)、ローカルリソースパッケージ(4.15 最小)、基本セキュリティ対策(4.16)、Hello World / cookbook / CLI 実行コマンド | M/S | v1 の統一検索で選んだリソースを安全に活性化・実行し、Policy・状態・監査ログまで通る。組込みライブラリとして実タスクを検証できる |
| **v3 — Advanced / Ecosystem** | セマンティック横断検索(4.2 F-RETR-02)、DAG 並列実行(4.6 F-PLAN-05)、高度な Agent Runtime(4.8 構造化タスク委譲)、チェックポイント / ロールバック(4.9 F-STATE-04)、補償処理(4.10 F-RECOV-05)、Execution Evaluator 強化(4.11 F-LOG-08)、OpenTelemetry optional、MCP 連携(4.13 / 4.15)、リソース署名 / 公開レジストリ、強隔離 L3(4.16 F-SEC-05)、サービス化 / 水平スケール、N対N マルチエージェントオーケストレーション | S/C | 本番運用、複雑な Agent 委譲、エコシステム拡大、規模・堅牢性・可観測性の高度化に対応する |

---

## 9. 非機能要件

| 区分 | 要件 |
|------|------|
| 低依存 | コアは標準ライブラリを優先し、必須の外部依存を必要最小限かつ監査可能に保つ。用途が限定的または重量級の外部依存は optional extra に隔離する |
| 拡張性 | リソース（Memory・Skill・Tool・Agent）/ 各エンジン / Connector はプラグイン・差し替え可能なインターフェースとする |
| 移植性 | 特定の LLM・クラウド・言語ランタイムにロックインされない設計とする |
| 可観測性 | すべての主要動作はログ・メトリクス・トレースで観測可能とする |
| 安全性 | デフォルトで最小権限（least privilege）。副作用は明示的許可がない限り実行しない |
| 信頼性 | 中断・再開・リカバリにより、部分的失敗から回復できる |
| 性能 | I/O バウンド処理を asyncio で並行化し、依存のないステップを並列実行できる |
| セキュリティ | 機密情報の秘匿管理・マスキング・改竄検知ログを備える |
| テスト容易性 | ドライラン・モック Connector / Agent・リプレイにより、副作用なしで検証できる |
| ドキュメント | OSS として採用されるために、API・拡張方法を体系的に文書化する（英日併記） |

---

## 10. 主要な相互作用（実行フロー例）

1. アプリケーション層がゴールを Kernel に投入する（`submit`）。
2. **共通リトリーバー & セットセレクター**（4.2）がゴールから Memory・Skill・Tool・Agent を横断検索し、必要な組み合わせを選択する。
3. **Policy Gateway**（4.3）がアクティベーションゲートを通過させるか判定する。
   - 許可 → アクティベーションへ
   - escalate → **人間確認**（4.12）へ（タスクは一時停止し `resume` を待つ）
   - 拒否 → 選択し直し or ステップ失敗
4. **リソースアクティベータ**（4.4）が型別に活性化する。
   - Memory → テキスト取得・コンテキストへ注入
   - Skill → 手順・制約をロード
   - Tool → 詳細スキーマを取得・利用可能ツール一覧に追加
   - Agent → Agent Card 取得・委譲インターフェース準備
5. **コンテキストコンパイラ**（4.5）が二段階 LLM 呼び出し用の入力を構築する。
   - 1回目: 軽量メタデータ → LLM がリソースを選択
   - 2回目: 活性化済みリソース → LLM が実際に処理
6. **実行計画**（4.6）がゴールをステップ列へ分解する。
7. 各ステップで **Policy Gateway** が実行ゲートを通過させるか判定する。
   - 許可 → **Tool Runtime**（4.7）または **Agent Runtime**（4.8）で実行
   - escalate → **人間確認**（4.12）へ
   - 拒否 → ステップ失敗
8. **Connector**（4.13）経由で外部環境と接続し、Tool / Agent を実行する。
9. 結果は **状態管理**（4.9）に記録され、チェックポイントが作られる。
10. 失敗時は **リカバリ**（4.10）がリトライ / 代替リソース検索 / 再計画 / エスカレーションを行う。
11. 全工程は **ログ・監査・実行評価**（4.11）に相関 ID 付きで記録され、Execution Evaluator がリソースの実績を Resource Registry にフィードバックする。

---

## 11. 用語集

| 用語 | 定義 |
|------|------|
| Kernel | エージェント実行の中核基盤。Marmo-Core 本体。 |
| Resource | Memory・Skill・Tool・Agent の4種類を総称する、カーネルが管理する能力・知識の単位。 |
| Memory | 検索可能な知識・経験・コンテキスト情報。実行時にプロンプトへ注入される（retrieve and inject）。 |
| Skill | 手順・ルール・ワークフローを記述した高位の再利用可能な能力。指示としてロードされる（retrieve and load）。 |
| Tool | 単一の実行可能な関数・API 呼び出し。呼び出し可能な関数として動的に登録される（retrieve and bind）。 |
| Agent | タスク委譲先となる専門エージェント。委譲インターフェース経由で接続される（retrieve and delegate）。 |
| Resource Registry | 4種類のリソースを統一メタデータで管理するレジストリ。 |
| Retriever | リソースレジストリに対する横断検索エンジン。 |
| Set Selector | 検索結果から最適な組み合わせ（Set）を選択するモジュール。 |
| Policy Gateway | 検索ヒット → 利用可能 → 実行許可 の多段階チェックを行うコンポーネント。旧 Guard を進化させたもの。 |
| Resource Activator | リソースを型に応じて実体化するモジュール。 |
| Context Compiler | 活性化済みリソースを LLM 入力（プロンプト + ツール定義）に変換するモジュール。 |
| Agent Card | Agent の能力詳細・入出力仕様・委譲インターフェース・対応タスク種別を記述したメタデータ。 |
| Agent Runtime | サブエージェントへのタスク委譲（ラップ委譲 / 構造化タスク委譲）を管理する実行環境。 |
| Tool Runtime | ツールの安全な実行（認証情報管理・スキーマ検証・タイムアウト）を管理する実行環境。 |
| Execution Evaluator | 成功率・コスト・レイテンシを記録・集計し、Resource Registry にフィードバックするモジュール。 |
| Plan | ゴールを分解した実行計画（ステップ列 / DAG）。 |
| Connector | 外部環境への接続を抽象化したコンポーネント。 |
| HITL | Human-in-the-loop。人間の確認・介入を介在させる仕組み。 |
| Checkpoint | 任意時点の状態スナップショット。ロールバックに使用。 |
| Escalate | 自動判断せず人間確認へ引き上げること。 |
| optional extra | コアに含めず、必要時のみ追加インストールする外部依存つき拡張。 |
| trust_level | リソースの信頼度分類（core / verified / community / untrusted）。 |

---

## 12. 今後の拡張（参考）

2.2「対象外」と重複しない範囲で、将来レイヤとして想定する拡張を記す。

**上位レイヤ / エコシステム**
- N 対 N マルチエージェント・オーケストレーション層
- 管理ダッシュボード / 可視化 UI（リソース利用状況・実行フロー可視化）
- MCP をはじめとする標準プロトコルとの完全な相互運用
- マルチテナント・課金基盤
- 学習ループ（実行結果から Retriever のスコアリングポリシーを改善）

**運用・スケール（利用実態が固まる v3 以降）**
- サービス / デーモン化、コンテナ公式イメージの提供
- 単一インスタンスでの複数タスク / 複数エージェント同時実行
- 共有 State Store を介した複数インスタンスでの水平スケール
- ヘルスチェック / readiness、グレースフルシャットダウン等の運用エンドポイント
- 永続状態のスキーマ・マイグレーション手順の整備

---

## 13. 未決事項（要レビュー）
- Python サポート下限（3.11 を仮置き。3.10 まで下げるか）
- 入出力スキーマに JSON Schema フルではなく軽量サブセットを自前実装する範囲
- 状態永続化の v2 最小実装を `json` ファイルと `sqlite3` のどちらで始めるか
- 監査ログのハッシュチェーンを v2 から入れるか v3 に回すか
- 同期 API と非同期 API のどちらを「主」とするか
- **[新] Memory リソースの保存・検索方式**: v1 ではローカル定義 + 全文検索 / `difflib` で十分か？ベクトル DB は v3 とするか？
- **[新] Set Selector のアルゴリズム**: v1 ではルールベース（権限・信頼度・副作用フィルタ後に上位 K 件）で十分か、v2 以降で LLM 判断を入れるか？
- **[新] Agent Runtime の v2 扱い**: v2 は Tool ラップ委譲までとし、構造化タスク委譲は v3 とするか？
- **[新] 二段階 LLM 呼び出し**: v2 から入れるか、v2 はルールベース選択を主として v3 で LLM 選択を強化するか？

---

## 14. ライセンス・ガバナンス・コミュニティ

世界的貢献者を集め、企業に安心して採用されるための非技術的基盤。技術仕様と同等に重要。

### 14.1 ライセンス
- **Apache License 2.0 を推奨**（要確定）。明示的な特許条項を持ち、企業の法務審査を通りやすく、基盤 OSS の事実上の標準。
- MIT は許容度が高いが特許条項がない。基盤・エージェント領域では Apache 2.0 の方が企業採用上有利。
- **F-GOV-01**: ライセンスをリポジトリルートの `LICENSE` に明記し、全配布物に適用する。

### 14.2 貢献・コミュニティ
初期は **BDFL / コア小チーム**で意思決定を高速化する。

- **F-GOV-02**: `CONTRIBUTING.md`（開発手順・コーディング規約・PR フロー）を整備する。
- **F-GOV-03**: `CODE_OF_CONDUCT.md`（行動規範）を整備する。
- **F-GOV-04**: **DCO（Developer Certificate of Origin / sign-off）** を採用し、貢献の出自を明確化する（CLA より貢献障壁が低い）。
- **F-GOV-05**: Issue / PR テンプレート、good-first-issue ラベル運用で新規貢献者を受け入れる。

### 14.3 API 安定性
- **F-GOV-06**: 公開 API・リソースメタデータスキーマに **SemVer** を適用し、メジャーバージョン内では後方互換を破壊しない。

### 14.4 セキュリティ運用
- **F-GOV-07**: `SECURITY.md` に脆弱性開示窓口と対応プロセスを明記する。

### 14.5 採用拡大後に検討する非技術基盤
以下は採用・貢献者が一定規模に達した段階で着手する。

- **ガバナンスの正式化**: ステアリング委員会 → 中立財団（例: LF AI & Data Foundation）への移管を視野に入れ、`GOVERNANCE.md` を整備する。
- **変更管理（RFC）**: 破壊的変更・新コンポーネント・公開 API 変更について、経緯を公開で残す RFC プロセスを導入する。
- **供給網の信頼性**: 署名付きリリース・SBOM 提供を整備する（F-DIST-04 と整合）。
- **透明性**: 公開ロードマップ（GitHub Projects / Discussions 等）でコミュニティ形成を促進する。

### 14.6 リポジトリ必須ファイル（チェックリスト）
現フェーズで必須: `README.md`（英日併記）/ `LICENSE` / `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md` / `SECURITY.md` / `CHANGELOG.md` / Issue・PR テンプレート / `要件定義書.md`（本書）
採用拡大後に追加: `GOVERNANCE.md`（14.5）

---

## 15. デプロイ形態

カーネルがまず「組込みライブラリ」として採用されるための最小限の形態・設定要件のみを定める。サービス / デーモン化、水平スケール、運用エンドポイント等の本格的な運用要件は、利用実態が固まる v3 以降の検討事項とし、本節では要件化しない（「12. 今後の拡張」参照）。

- **F-OPS-01**: 第一の利用形態として**組込みライブラリ**（同一プロセス内、最小依存・最小構成）をサポートする。スタンドアロン・サービス / デーモン、コンテナ公式イメージは将来の拡張形態とする。
- **F-OPS-02**: 設定は環境変数 / 設定ファイルで与えられ、コード変更なしに切替できる（12-factor 準拠）。
- **F-OPS-03**: 永続化された状態（4.9）にスキーマバージョンを持たせ、カーネル更新時に移行可能な余地を残す（マイグレーション手順の整備は v3 以降）。

> クラッシュからの再開は状態永続化（4.9 F-STATE-02）として、リソース上限の強制は Policy Gateway（4.3 F-GATE-06）として既に要件化済みであり、本節では重複して定義しない。
