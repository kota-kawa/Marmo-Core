interface:
  display_name: "Flow Pilot"
  short_description: "Single-tick autonomous build-loop conductor — one ready spec, one stage per tick, terminal PILOT_VERDICT line"
  brand_color: "#3B82F6"
policy:
  allow_implicit_invocation: false
