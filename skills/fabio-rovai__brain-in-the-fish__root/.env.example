# Optional: enables LLM-enhanced scoring via Claude API
# Without this, the system runs in deterministic-only mode (evidence scorer, no LLM)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default model for LLM scoring
# LLM_MODEL=claude-sonnet-4-20250514
