# Marmo-Core 改善策（逐次更新）

優先度が高い、または効果が大きいものだけを載せています。確認が進むにつれて追記します。
専門用語はできるだけ避け、必要な場合は（ ）で補足しています。

---

## 1. 設計と使いやすさ

- **非同期処理（同時に複数の待ち時間を重ねて処理するしくみ）に対応していない【最重要】**
  現在の Marmo-Core は「一つ終わるまで次を待つ」方式だけで作られています。ところが今の Web サービスや AI エージェントの多くは、非同期方式で動く土台（Web フレームワークや MCP サーバーなど）の上に作られます。そこへ Marmo-Core を組み込むには毎回「別スレッドに逃がす」細工が必要になり、採用の障壁になります。設計書でも「非同期で並行化する」と定めているので、まず非同期版の中核（カーネル・LLM 接続・ツール実行）を用意し、従来の同期版はその上に薄く載せる形に変えるべきです。

- **公開している機能の一覧が大きすぎ、どれが「安定」なのか分からない**
  利用者が最初に目にする公開一覧に約 190 個の名前が並び、内部向けに近い部品まで一括で外に出しています。まだ 0.x 版なのに「どこまでが将来も変えない約束か」が読み取れません。「安定して使ってよい中核（カーネル、資源登録、検索、選択、権限設定、人間確認、LLM 接続）」と「まだ変わりうる実験的な部分」を分け、文書でも明示することを推奨します。

- **中核ファイルが約 1,900 行と巨大で、1 つの処理の中に多くの段階が同居している**
  検索 → 選択 → 起動 → 文脈組み立て → モデル呼び出し → 計画実行 → 復旧、といった段階が一つの長い処理の中に詰め込まれ、途中経過の保存・復元の情報も各所に散らばっています。機能追加や不具合調査のたびに全体を読む必要があり、不具合の温床になります。段階ごとに部品を分け、「途中経過の保存と復元」を一か所に集めるべきです。

- **「4 種類の資源（記憶・スキル・道具・エージェント）を統一して扱う」と掲げているのに、スキルだけ見本と実行例が無い**
  README や更新履歴でも「記憶・道具・エージェント各 10 件」とあり、スキルの見本・説明・実行例がありません。一方、Anthropic の Agent Skills や OpenClaw などで広まっている「SKILL.md 形式」を読み込む機能自体は存在しています。今もっとも需要が高いのはまさに「手元の SKILL.md をそのまま安全に検索・実行できる」体験なので、スキルの見本を 10 件そろえ、README の冒頭にその使い方を置くべきです。

- **README が英語だけで、日本語版が無い**
  プロジェクトの方針（英日併記）と設計書の非機能要件に反しています。少なくとも README と docs 配下の 3 文書に日本語を併記してください。

- **「30 秒で最初の成功体験」という目標が達成できていない**
  同梱の最小サンプルでは、足し算をする道具を 1 つ動かすだけで、15 項目ほどの説明情報（入力の要約、出力の要約、遅さの分類、参照先 など）を手書きした約 90 行のコードが要ります。競合（OpenAI Agents SDK、LangChain、PydanticAI）は関数 1 つに印を付けるだけで済みます。関数の型や説明文から必要な情報を自動生成する「印を付けるだけ」の書き方を用意し、README 冒頭を 10 行以内の例にすることが、採用率に最も直結します。

- **ライブラリとして組み込む方法が README にひとつも書かれていない**
  README のコード例はすべてコマンドライン操作で、Python から中核を呼び出す例、全体の流れ図（検索 → 関門 → 起動 → 文脈組み立て → モデル → 実行 → 監査）、生成された API リファレンスのいずれも無く、同梱サンプルへのリンクもありません。採用を検討する人は、ソースコードを読まないと「自分のアプリにどう組み込むか」が分かりません。README に「ライブラリとして使う」節、docs に全体構成図、安定部分の API リファレンス自動生成を追加してください。

- **SKILL.md の読み込みが実在のスキルに対して不完全**
  収集済みの 812 件の実在スキルで確認すると、先頭の設定欄（frontmatter）の入れ子構造を 1 行の文字列に潰してしまい、94 件で「作者・版」などの情報が壊れます。また 179 件が持つ「使ってよい道具の一覧（allowed-tools）」を読んではいるものの、権限の関門にまったく結びつけていません。これは「他人のスキルを安全に実行する」という売りの要となる情報なので、入れ子対応の読み込みと、許可道具一覧を必要権限へ写す処理が必要です。

- **MCP はクライアント側だけで、サーバー側（Marmo 自身を道具として公開する）が無い。他基盤への接続部品も無い**
  Claude Code / Cursor / Codex などから Marmo を使うには「Marmo が MCP サーバーとして立つ」必要がありますが、その機能がありません。OpenAI 形式への道具定義の変換も内部に閉じていて再利用できず、LangChain / LangGraph 向けの接続部品も無いため、既存のアプリに「下に敷く」形で導入する経路が塞がっています。

- **pip で入れた利用者が README の手順をそのまま実行できない**
  README が参照する見本の JSON ファイル（記憶・道具・エージェント各 10 件）は配布パッケージに含まれておらず、リポジトリを丸ごと取得した人しか手順を再現できません。また `--version` で版を確認する手段が無く（部分コマンドの別の意味のオプションと衝突）、初見で戸惑います。見本をパッケージに同梱して 1 コマンドで展開できるようにし、版表示を追加してください。

- **設定の読み込みが「今いるフォルダ」に依存し、唯一の外部依存（dotenv）が必須になっている**
  LLM 接続部品を作るたびに、実行中のフォルダから `.env` ファイルを探して読む動きになっており、サーバーの中やテスト実行時に、起動場所によって挙動が変わります。`.env` を一切使わない利用者にも唯一の外部依存が強制されています。`.env` 読み込みはコマンドライン側だけに限定し、ライブラリ側は明示的な設定オブジェクトで受け取る形にしてください。

- **品質保証の網に穴がある**
  テストは 419 件すべて成功、カバレッジ 87% と良好ですが、(a) CI にカバレッジの下限ゲートが無い、(b) 型検査は数値計算ライブラリが入っている環境だと失敗する（CI では偶然入っていないため通る）、(c) 静的検査の規則が最小限、(d) テストが「版ごと」に分かれていてモジュール単位の検証が薄く、採用の入口にあたるコマンドライン（62%）・読み込み・SKILL.md 処理が最も薄い、という状態です。

---

## 2. 研究としての新しさと、結果の再現性

- **ベンチマーク（性能測定）が、リポジトリに含まれていない 261MB の資料に依存しており、第三者が再現できない**
  性能測定の既定データは、GitHub から収集した約 1,000 件の実在するスキル文書ですが、これは Git 管理から除外されています。そのため README に載っている「1 位正解率」「上位 50 件再現率」などの数値を、外部の人が検証できません。研究上の主張として信頼を得るには、少なくとも「どこから・どの版を・どのライセンスで取得したか」の一覧表だけをリポジトリに含めて同じデータを自動復元できる仕組みを用意する、または（許諾ライセンスのものに絞っているので）データセットとして公開することが必要です。

- **「実在するスキル 1,000 件 + 正解ラベル付きの課題 120 件」は、それ自体が発表価値のある資産**
  現在は性能測定の内部資料として埋もれています。エージェント用の道具選択ベンチマークとして知られる ToolBench、API-Bank、BFCL などは、SKILL.md 形式の実在スキルを扱っていません。「言い回しの難しさ 3 段階」「複数正解を許す採点」「日本語の課題文」を備えたデータセットとして切り出せば、新規性を明確に主張できます。あわせて「誰が・どういう基準で正解を付けたか」と、複数人で付けた場合の一致度を記録する必要があります。

- **比較対象が自前実装どうしだけで、外部の標準的な比較相手が無い**
  BM25、埋め込み併用、仮想文書生成、LLM による並べ替え、いずれも本ライブラリ内の実装同士の比較です。「絞り込みをしない（全部の道具をそのまま渡す）」場合や、OpenAI / Anthropic 本体の道具選択、既存の広く使われる検索ライブラリとの比較が無いため、「絞り込みによって最終的な仕事の成功率が上がる」という肝心の主張が検証されていません。検索の当たり外れだけでなく、**最終的な仕事の成否・使ったトークン量・費用・所要時間** を測る測定を 1 本追加すべきです。

- **最も独自性の高い「組み合わせ選択（依存関係・競合・予算を満たす資源の組を選ぶ）」の評価が、自分で作った合成データだけ**
  合成データは仕様に都合よく作れるため説得力が弱いです。実在スキルの上に依存・競合の注釈を付けた小規模（50〜100 件）の実データで、貪欲法・ビーム探索・分枝限定法の「最適解との差」と「計算時間」を報告してください。

- **性能測定の設計に「正解が漏れている」「テストデータで調整している」問題があり、数値がそのままでは論文の査読に耐えない**
  (a) 組み合わせ選択の測定では「各種類から 1 件ずつ選ぶ」という上限を測定側が与えており、これは正解の構造そのものを教えているのと同じです（比較相手の LLM 選択はこの恩恵を受けられず不利）。(b) 埋め込みの重み 0.9、経路探索の各種しきい値、事例再利用のしきい値 0.40 は、いずれも同じ評価データで最良を選んだ値で、未見のデータでの性能を示していません。(c) 件数が少なく（組み合わせ選択 28 件、うち「見送り／人間確認」は 4 件と 2 件）、信頼区間も無いので「100% 対 50%」といった差は偶然と区別できません。(d) 正解判定が「ID に部分文字列が含まれるか」で、120 件中 57 件は複数の ID を正解として受け付けるため、一般的な「1 位正解率」と比較できません。(e) 「階層化で 43 倍高速」という主張の比較相手は純 Python の総当たり計算で、数値計算ライブラリや近傍探索ライブラリを使った標準的な相手と比べれば差はほぼ消えます。評価データを学習用と検証用に分け、ブートストラップで信頼区間を付け、上限指定を外した実験を追加してください。

- **利用実績から学ぶ経路選択（過去の成功事例を再利用するしくみ）に、権限が変わったときの判定漏れがある**
  過去に「権限不足で人間確認に回した」事例を再利用するかどうかを判定する際、「今の権限が過去の権限を完全に含んで増えたか」だけを見ています。そのため、過去とはまったく別の権限を新たに持っている場合（まさにその権限で実行可能になるケース）でも、古い「見送り」の判断がそのまま再利用されます。「過去に無かった権限が一つでもあれば再判定する」に直すべきです。

- **検索の点数付けに、順位を逆転させる計算上の欠陥がある**
  (a) 埋め込み（意味ベクトル）の類似度を 0〜1 に変換する式が、0 をまたぐ所で不連続かつ単調でなく、「より似ていない文書」の方が高い点になる場合があります（類似度 −0.01 → 0.495、+0.2 → 0.2）。ハッシュ式埋め込みが BM25 に負けている一因の可能性があります。(b) **【2026-09-19 修正済み】** 文字一致（BM25）の点を「その検索での最高点」で割って正規化していたため、まったく無関係な問い合わせでも 1 位の候補は必ず高い点になり、「該当なしとして見送る」判定のしきい値が機能しませんでした。「問い合わせの語をすべて 1 回ずつ含む文書」を基準にした絶対値へ変更（ラーメンの質問に対する 1 位が 0.63 → 0.15）。順位は変わらないことを 120 シナリオで確認済みです。(c) 交差エンコーダ（高精度な並べ替え器）の生の出力をそのまま 0〜1 に押し込んでいるため、ほぼ 0 か 1 の二値になり、しきい値判定と噛み合いません。(d) 日本語・中国語以外の非 ASCII 文字（ドイツ語・フランス語のアクセント付き文字、ハングル、全角英数）が分割時にすべて捨てられ、「Übersetze」が「bersetze」になるなど、欧州言語の資料が検索できません。日本語も「書を」「を作」のように助詞をまたぐ 2 文字組が生まれ、雑音になります（この雑音は残っており、日本語の無関係な質問が今も 0.34 程度の関連度を得ます）。

- **スキル本文を宣言情報と同じ重みで索引していたため、種類をまたいだ比較が壊れていた【2026-09-19 修正済み】**
  SKILL.md の本文（中央値 667 語、最大 7,551 語）を、道具の宣言情報（同 34 語）と同じ重みで検索対象にしていたため、本文がたまたま語を含むだけのスキルが、その仕事のために作られた道具に勝っていました（「read a local text file safely」で XXE 検査スキル 0.790 対 読み取りツール 0.579）。宣言情報と本文を別の欄として扱い、本文の重みを 0.25 に下げて解消（同じ問い合わせで読み取りツールが 1,030 件中 1 位、hit@1 61.7% → 65.0%）。

- **性能面: 統計情報を書き戻すたびに索引と埋め込みをすべて作り直している【2026-09-19 修正済み】**
  資源の利用実績（成功率など）を更新すると登録簿の「版番号」が上がり、それを鍵にしている検索索引と埋め込みの保存が全部無効になっていました。検索対象の文章は変わっていないのに、63 件で実験しただけで埋め込みの再計算が倍になり、1 万件を外部の埋め込み API で処理していれば実績更新のたびに全件を再課金することになります。「検索が読む情報が変わったときだけ上がる版番号」を追加し、索引・埋め込み・階層化の分類・能力グラフをそちらに紐付けて解消しました（実 API で検証: 検索 → 再検索 → 実績書き戻しで 5 リクエスト → 2 リクエスト）。階層化検索が「選ばれた分類の組み合わせ」ごとに登録簿を複製する点は未対応です。

- **検索の細かな無駄【2026-09-19 修正済み】**
  (a) キーワード指定付きの検索が、候補ごとに全文を組み立て直していたため、1,000 件で検索全体の 22 倍（3.3ms → 74.9ms）かかっていました（索引側に保持して 17.4ms）。(b) 1 つのゴールにつき検索が 4 回走っており、埋め込みや LLM を使う検索器では同じ文章に対する課金往復が 4 回発生していました（2 回に削減）。

- **性能測定の説明書が日本語だけ、本体 README は英語だけ**
  研究成果を海外に届けたいなら、性能測定の説明書にも英語版が必要です。

---

## 3. 安全に実行するしくみ

- **「安全に実行するカーネル」を掲げつつ、道具は同じプロセスの中でそのまま動き、隔離されていない**
  道具は同じプログラム内の別スレッドで直接呼び出され、時間切れになってもスレッドを放置するだけで、強制停止も、メモリや CPU の使用量制限もありません。「隔離レベル L0〜L3」という区分は宣言の突き合わせだけで、実体としての隔離（別プロセスでの実行や作業フォルダの制限など）は存在しません。最低限「別プロセスで実行し、時間切れで強制終了し、資源の上限と作業フォルダを制限する」レベルを標準実装し、宣言と実際の動きを結びつけないと、差別化軸「安全性ファースト」が名目上のものになります。

- **資源の定義ファイルが「どの Python 関数を動かすか」を自己申告でき、信頼度も自己申告なので、ダウンロードした資源で任意のコードが実行できてしまう【最重要の安全上の欠陥】**
  資源定義（JSON）の参照先に「python:モジュール:関数」と書けば、Marmo は許可一覧なしにその関数を取り込んで実行します。同じファイルに「信頼度: 中核」「副作用: なし」「必要権限: なし」と書けば、そのまま信用されます。実際に確認したところ、OS のコマンド実行関数を指した定義が検証を通り、初期設定の関門を「許可」で通過し、ファイルを作成できました。安全性方針の文書自身が「ダウンロードした資源による、運用者が許可していない実行」を対象範囲としているので、これは看板と実態の食い違いです。対策は、(a) 参照できるモジュールを運用者が明示的に許可した一覧に限る（または事前登録済みの実装しか使えないようにする）、(b) 信頼度はファイルの記述ではなく「どこから来た資源か」から Marmo 側が決める、(c) 入力の形式指定で「未定義の引数を拒否する」を既定にする、の 3 点です。

- **記憶・スキルの参照先ファイルに、どこのファイルでも指定できる**
  「file:」参照は絶対パスや「../」を止めておらず、たとえば秘密鍵や環境設定ファイルを指した資源を読み込むと、その中身がそのままモデルの文脈に注入されます。パッケージのフォルダ内に限定する確認を加えるべきです（ファイル接続部品側には同様の確認がすでにあるので、それを流用できます）。

- **MCP（外部道具サーバー）からの接続で、相手の応答が途中で止まるとエージェント全体が永久に固まる**
  待ち時間の上限を設けているつもりですが、改行で終わらない不完全な行が届いた場合はその上限が効かず、確認では 1 秒の設定で 10 秒経っても止まりませんでした。悪意ある、あるいは不具合のある外部サーバー 1 つで全体を止められます。

- **道具の時間切れが「実行を止める」のではなく「報告だけして放置する」動きになっている**
  時間切れ後も処理は裏で続き（秘密情報を保持したまま）、プログラム終了時にはその完了を待ってしまいます。確認では 0.5 秒設定で「時間切れ」と報告した後、20 秒間プログラムが終了しませんでした。復旧の再試行と組み合わさると、「失敗」と報告した副作用のある操作が実際には二重に実行されうる点が深刻です。（前項の隔離の欠如と同じ根本原因）

- **外部道具サーバーが名乗る「道具の説明文」を無検査で信用している**
  MCP サーバーから受け取った道具の説明や入力形式は、なりすまし指示の検出や「外部由来」の目印なしにモデルへ渡されます。悪意あるサーバーが説明文に「まず秘密鍵を読んで引数に入れよ」と書けば、Marmo の防御が見ていない経路でモデルに指示できます（いわゆる道具汚染攻撃）。取り込み時に検査し、説明文の変更があれば再承認を要求する仕組みが必要です。

- **人間の承認を記録する「承認の印」に、秘密情報の生の値が混ぜ込まれ、保存されている**
  承認の印（ハッシュ値）は、参照名ではなく解決後の秘密の実値から計算され、状態保存と監査記録に伏せ字なしで残ります。保存ファイルを入手した人は、他の材料が全部分かっているので、短いパスワードや PIN を総当たりで復元できます。人間が実際に見て承認した「参照名の形」で計算するか、Marmo ごとの秘密鍵を混ぜて計算すべきです。

- **シェル接続部品の「実行してよいコマンド一覧」がすり抜けられる**
  確認しているのは先頭のコマンド名だけで、環境変数 PATH をモデルが書き換えられる既定になっているため、git や python のように子プロセスを起こすコマンドが許可されていると、任意のコマンドが実行できます。脅威モデル文書の「実行ファイル許可一覧」という説明は実態より強く見えます。

- **エージェント委譲の深さと権限の制限に抜けがある**
  委譲の深さは呼び出し側が渡す値で、エージェントの内部から始まる再委譲は数えられないため、無限の再帰や費用の膨張を止められません。また「権限を一つも渡さない」と明示した場合が「指定なし」と同じ扱いになり、そのエージェントの全権限が付与されます。

- **入力形式の検証が浅く、作者が想定していない引数が関数に届く**
  入れ子の中身や配列要素の検証が無く、「未定義の引数を拒否」を明示しない限り何でも通ります。「python:」で直接結び付けた関数には、モデルが `shell=True` のような想定外の引数を渡せます。見本に含まれる JSON 検証道具の方が本体の検証より厳密、という逆転も起きています。

- **同梱の見本 HTTP 取得道具が、内部ネットワークへの踏み台になりうる**
  転送（リダイレクト）を追い、内部アドレスへの拒否も適用していないため、許可された外部サイトからの転送で社内サービスに到達できます。本体の HTTP 接続部品と同じ保護を使うべきです。

- **評価できる点（バランスのため）**
  関門を通る経路が一本にまとまっていて迂回できない、秘密情報は参照名のまま扱い出力や誤り文から伏せ字にする、外部由来の内容を読んだ後の副作用は再承認を要求する、監査記録が改ざん検知付き、接続部品（HTTP・ファイル・シェル・SQLite）は一般的なエージェント基盤より慎重に作られている、といった土台は堅実です。上の問題は主に「資源ファイルの自己申告を信用している」「同じプロセス内で動かしている」という継ぎ目に集中しており、中核の関門の論理そのものの欠陥ではありません。

- **三段階の関門のうち「出力の関門」が定義だけあって、実際には一度も使われていない**
  設計上は「起動時」「実行時」「出力時」の 3 つの関門があり、コマンドラインの選択肢にも「出力」が出てきますが、道具が結果を返した後にそれを検査する処理がどこにもありません。出力の形式指定も、モデルに見せるためだけに使われています。道具の出力について、形式の検証・サイズ上限・機密情報の混入チェックを行う「出力の関門」を実装し、説明書との食い違いを解消してください。

- **なりすまし指示（プロンプトインジェクション）対策の粒度が粗く、運用で「承認疲れ」を起こす設計**
  検出は定型パターンの照合だけです。さらに、モデルが一度でも外部由来の内容（あらゆる道具の結果を含む）を読むと、その後の「書き込み・外部通信・取り消し不能」の操作が **出所を問わず全て人間確認に回る** 仕様です。道具を 1 回でも使う仕事は実質「毎回人間に確認」になり、確認が形骸化するか、機能を無効化されてしまいます。改善案は、(a)「どの道具の結果が、どの引数に流れたか」を追跡し、汚染された引数を含む呼び出しだけ確認に回す、(b) 検出器を差し替え可能にして、LLM を使った判定器や外部の安全判定サービスを任意に接続できるようにする、の 2 点です。

---

## 4. ビジネス観点

- **狙う顧客は「規制業界・監査を重視する企業」なのに、その顧客が最初に確認する材料が不足**
  改ざん検知付きの監査記録、人間確認、出来事の完全な記録と再生、という強みはあります。しかし、(a) 脅威モデルの文書が 75 行と簡素、(b) SOC2 / ISO 27001 / EU AI 法 / NIST の AI リスク管理枠組みなどの統制項目と機能との対応表が無い、(c) 監査記録が独自形式で、企業の監視基盤（Splunk、Datadog、OpenTelemetry）に流し込めない、という状態です。「統制項目との対応表」1 ページと、監視基盤への出力機能（任意インストール）を用意すると、企業の試験導入の入口が開きます。

- **収益や持続のモデルが見えない（無償公開のまま）**
  ライセンスは Apache 2.0、案内先は会社サイトですが、有償サポート・ホスト版・企業向け機能（署名付きの資源配布、集中管理された権限設定、監査画面）への言及がありません。設計書で「管理画面」「複数顧客の同居」を対象外としているのは妥当ですが、それらはそのまま「無償部分と有償部分の境界」になります。早めに **中核（無償）／配布基盤・管理画面（有償）** の線引きを明記し、無償部分の設計がその境界を壊さないよう、今のうちに監査記録の出力先などを差し替え可能にしておくべきです。

- **市場での認知経路が弱い**
  PyPI 公開から約 3 週間、コミット 33 件、開発者 1 名です。競合の多い分野（LangGraph、OpenAI Agents SDK、Semantic Kernel、CrewAI、PydanticAI）で「また一つエージェント基盤が出た」と見られると埋没します。差別化を **「既存の基盤の下に敷く、安全実行の層」** に絞り、LangGraph の部品として、OpenAI Agents SDK の道具として、MCP サーバーとして「包んで使える」接続部品を最優先で出す方が、置き換えを迫るより導入障壁が低いです。特に **Marmo の権限確認・監査・人間確認を通した MCP サーバー** は Claude Code / Cursor / Codex の利用者に直接届き、最小工数で最大の露出になります。

- **1,000 件スキル資料の権利と帰属のリスク**
  収集時に許諾の広いライセンスに絞ってはいますが、再配布時に必要なライセンス文と著作権表示（MIT や BSD には表示義務があります）を一覧に自動でまとめる処理が無く、データセット公開時に問題になりえます。公開前に権利表示の自動生成を追加してください。逆に整備できれば「実在エージェントスキルのデータセット提供者」という立ち位置が取れ、研究・企業双方からの認知獲得（論文・記事・登壇）に使えます。

- **日本市場での優位性を活かしていない**
  開発者・設計書・性能測定の説明が日本語である一方、成果物は英語 README のみで日本語利用者に届かず、逆に研究成果は日本語だけで海外に届きません。日本語の課題文での検索品質（日本語向けの分割処理は実装済み）を **日本語ベンチマークとして数値化** し、「日本語に強い安全実行カーネル」と打ち出せば、国内企業（金融・製造・公共）の試験導入に直結しやすいです。README の英日併記はこの観点でも必須です。

- **版の方針と保守の約束が未記載**
  0.4.0 で「ベータ」ですが、公開機能の互換性方針、保守期間、脆弱性修正の目標応答時間（窓口はあるが目標が無い）が書かれていません。企業の稟議では「1.0 までの道筋」と「機能の安定性宣言」が求められます。更新履歴の「未リリース」欄に 1.0 到達条件（非同期対応、出力の関門、実体ある隔離、公開機能の凍結）を明文化してください。

---

## 5. 「OpenClaw 級のインパクト」を狙う場合の戦略

前提認識: OpenClaw が爆発的に広がった要因は「中身の出来」ではなく、(1) 毎日使うチャットアプリ（WhatsApp / Telegram / Discord など）からすぐ動く **個人向けの体験**、(2) SKILL.md 形式の **スキル共有の生態系（ClawHub）**、(3) 手元の機械で動かせて自分のデータを扱えること、(4) SNS や動画で映える「勝手に仕事が片付く」デモ、でした。一方で公開直後から **外部にさらされた入口、悪意あるスキル、秘密情報の漏洩、なりすまし指示** といった安全上の問題が繰り返し報じられ、「便利だが怖い」という印象が定着しました。Marmo-Core の強み（関門・監査・人間確認・隔離の宣言・なりすまし指示の検出）は、まさにその「怖い」の解消に対応します。ただし **ライブラリのままでは同種のインパクトは出ません**。以下は優先順です。

- **最優先: 「OpenClaw / Claude Code / Cursor のスキルを、入れる前に安全検査する」1 コマンドの製品面を作る**
  すでに 1,000 件超の実在 SKILL.md（OpenClaw 由来も含む）を収集する仕組み、SKILL.md を読み込む機能、なりすまし指示や危険操作の検出器、権限の関門がそろっています。これを「スキルのフォルダや URL を渡すと、危険な副作用・秘密の持ち出し・なりすまし指示・未申告の外部通信を点数付きで報告する」1 コマンドとして切り出せば、「ClawHub のスキルを入れる前に必ず通す道具」として OpenClaw 利用者に直接届きます。拡散力が高く、実装は既存部品のつなぎ直しが中心です。

- **OpenClaw そのものへの組み込み（置き換えではなく、下に敷く）**
  OpenClaw の入口やスキル実行の手前に、Marmo の三段階の関門と監査記録を差し込む「OpenClaw 向け安全化プラグイン」を出します。導入者は今の体験を失わず「安全になる」だけなので摩擦が最小です。ここで得た実運用の記録（実際の道具呼び出しの分布、拒否率、人間確認の頻度）は、そのまま研究データになります。

- **看板となるアプリを 1 本用意する（中核部品だけでは映えない）**
  例:「監査記録と人間確認付きで、Slack / LINE / Telegram から家計簿・メール・カレンダーを扱う個人秘書」。OpenClaw との差は **「何をされたか全部見える・止められる・元に戻せる」** に置きます。Marmo-Core には外部接続（HTTP・ファイル・シェル・SQLite）と状態保存があるので、足りないのはチャットアプリ接続と 30 秒で終わる初期設定だけです。デモ動画は「エージェントが怪しい指示を読んだ瞬間に関門が止め、利用者がスマホで承認する」場面を核にします。

- **スキル生態系への供給側になる**
  収集済みの資料を整備し「安全性スコア付きスキル一覧」（各スキルの副作用・必要権限・なりすまし指示の検査結果を付与）として公開すれば、既存の配布所に無い価値になり、検索流入とコミュニティ形成の起点になります。前述の権利表示の整備が前提です。

- **技術者の心を掴む「数字」を 1 つ持つ**
  OpenClaw は GitHub のスター数が象徴でした。Marmo は「同じ 1,000 スキルで、悪意あるスキル X 件を実行前に停止／なりすまし指示 Y 件を検出／文脈のトークンを 99% 削減」のような **安全性 × 効率の数値** を案内ページと README の冒頭に置きます。第 2 章の再現性整備はそのままここに効きます。

- **やらないことを決める**
  OpenClaw と同じ土俵（機能数・対応チャネル数）で競うと単独開発では勝てません。「エージェントが動く土台の安全と監査は Marmo」という一点に集中し、OpenClaw / Claude Code / MCP の生態系全体に相乗りする方が到達距離は長くなります。

---

## 6. 総括: まず何から手を付けるか（優先順）

1. **安全の看板と実態を一致させる**（第 3 章の先頭 2 件）: 「python: 参照の許可一覧」「信頼度をファイルの自己申告にしない」「file: 参照の範囲制限」。いずれも数日規模で、放置すると「安全なカーネル」という主張自体が反証されます。
2. **30 秒で動く体験と、ライブラリとしての組み込み口**（第 1 章）: 印を付けるだけの道具定義、README 冒頭 10 行の例、非同期対応の入口、MCP サーバー化。認知と採用に直結します。
3. **スキル安全検査の 1 コマンド化**（第 5 章）: 既存部品の再配線で作れ、OpenClaw / Claude Code 利用者に届く最短の製品面です。SKILL.md 読み込みの修正（入れ子・許可道具一覧）はこの前提です。
4. **研究の再現性と評価設計の立て直し**（第 2 章）: データセットの公開手順、学習用と検証用の分離、信頼区間、正解漏れの除去、外部ベースライン。計算上の欠陥（類似度の変換、正規化、事例再利用の判定）はこの前に直します。
5. **企業向けの説明資料と収益の線引き**（第 4 章）: 統制対応表、監視基盤への出力、1.0 到達条件と互換性方針、無償と有償の境界。

---

## 7. 攻めの改善策（人を呼び込み、立ち位置を取るためのもの）

第 1〜4 章の多くは「信頼を失わないための守り」です。ここでは、認知・利用者・立ち位置を積極的に取るための施策を、Marmo-Core が今すでに持っている部品から作れるものに限って挙げます。おおむね効果の大きい順です。

### 7-1. 数字で世間の注目を取る

- **「実在スキル 1,000 件を検査したら、何 % に危険な指示・未申告の外部通信・秘密情報の持ち出しがあったか」という調査レポートを公開する**
  すでに収集済みの資料と検出器で、今すぐ作れる唯一の「ニュースになる数字」です。OpenClaw の事故報道と重なる文脈で、技術記事・SNS・報道の入口になります。レポートの最後に「Marmo の検査コマンドで自分のスキルも確かめられます」と置けば、そのまま利用者獲得につながります。研究発表（論文投稿）と同時に出せば信頼性も担保できます。

- **公開の順位表（リーダーボード）を運営する**
  自作の性能測定を、他の人が自分の検索・選択方式を投稿して競える公開ベンチマークに変えます。順位表は参加者が宣伝してくれる仕組みであり、「エージェント資源の選択ならこの評価が標準」という立ち位置を取れます。

### 7-2. 製品面を作り、体験で伝える

- **スキル安全検査の 1 コマンド**（第 5 章で既述。攻めの中核）
  結果は点数・理由・「入れてよいか」の一言で返す。公開スキル配布所の URL を直接渡せるようにし、検査結果を共有しやすい 1 枚の画像や短い URL で出すと拡散します。

- **「AI エージェントの取り消しボタン」を売りにする**
  Marmo は出来事をすべて記録し、検査点まで戻し、副作用の埋め合わせを実行できます。これを「エージェントがやったことを、後から巻き戻せる」体験として見せます。他の基盤に無く、動画で伝わりやすい機能です。

- **「フライトレコーダー」画面**
  1 回の実行で「何を検索し、何を選び、どの関門で止まり、人が何を承認し、何が動いたか」を時系列で見られる、ブラウザで開くだけの 1 枚の画面を出します。監査記録と状態保存はすでにあるので、見せ方を作るだけです。技術者向けデモ動画の主役になり、企業の「見える化」要求にも直結します。

- **承認はスマホで**
  人間確認を LINE / Slack / Telegram に送り、その場で「許可・却下・条件付き許可」を返せるようにします。「エージェントが怪しい指示を読んだ瞬間に止まり、スマホに通知が来て、承認すると再開する」場面が、Marmo を一言で説明する動画になります。

- **既存の道具サーバーの前に立つ安全な中継役**
  Claude Code / Cursor / OpenClaw の設定を 1 行差し替えるだけで、いま使っている外部道具サーバー（MCP）の呼び出しすべてに、関門・監査・人間確認が付く中継役を出します。利用者は何も作り替えずに導入でき、「下に敷く」戦略の最短経路です。

### 7-3. 生態系の「決める側」に立つ

- **SKILL.md の安全欄の仕様を提案し、その提案者になる**
  いまのスキル形式には「どんな副作用があるか」「どの権限が要るか」を書く決まった欄がありません。Marmo が使っている項目（副作用・必要権限・信頼度・隔離レベル）を「スキルの安全宣言」として仕様提案し、公開の仕様サイトや OpenClaw 側に取り込んでもらえれば、Marmo はそれを検証する標準の道具になります。仕様の作り手になることは、機能で競うより強い立ち位置です。

- **署名付きスキル配布と「検証済み」の印**
  資源パッケージの署名と検証の土台はすでにあります。スキル作者が自分の配布物に署名し、Marmo の検査に通った印（README に貼れるバッジ）を出せるようにすると、作者自身が Marmo を宣伝してくれます。ソフトウェア供給網の安全（npm や Sigstore の流れ）を、エージェントスキルに持ち込む先駆けになれます。

- **安全性スコア付きスキル一覧（公開カタログ）**
  収集済みのスキルに検査結果を付けて検索できる形で公開します。「スキルを探すならここ」という導線ができ、上記のバッジ・仕様提案・検査コマンドがすべてここに集まります。

### 7-4. 研究を認知の武器にする

- **「道具の選択は組み合わせ問題であり、上位 K 件や LLM 任せでは体系的に失敗する」という主張で論文を出す**
  第 2 章の指摘（正解漏れ・信頼区間・外部比較）を直した上で、独自性のある「見送り・人間確認を含む組み合わせ選択」を評価論文としてまとめます。査読を通れば、企業向け説明・採用・採択の全部で「根拠がある」と言えるようになります。データセット公開と同時が最も効果的です。

### 7-5. 日本市場での先行

- **日本語スキルの安全検査と、日本語での性能数値を先に出す**
  国内の生成 AI ガイドライン（金融・医療・公共）に対応した統制対応表と、LINE での承認、日本語の課題での検索精度を合わせて「日本語で使える安全なエージェント基盤」として先に旗を立てます。海外勢が日本語対応を後回しにする間に、国内企業の試験導入を取れます。

### 攻めの施策の順序（提案）

1. 安全修正（第 3 章の先頭 2 件）を済ませる。これ無しで攻めると逆効果になる。
2. 1,000 件スキル検査レポート + 検査コマンドを同時公開。最初の「数字」と「道具」を一度に出す。
3. 中継役（MCP プロキシ）と OpenClaw プラグインで「下に敷く」導入経路を開く。
4. フライトレコーダー画面とスマホ承認で、動画で伝わる体験を作る。
5. 仕様提案・バッジ・公開カタログで生態系の決める側に回る。
6. 論文とデータセットで裏付けを固め、企業向け・日本市場の展開に使う。

---

## 8. 攻めの施策の詳細計画: 「1,000 件検査レポート + 検査コマンド」と「フライトレコーダー + スマホ承認」

第 7 章の順序 2 と 4 について、何を作るか・すでにあるもの・足りないもの・手順・成果物・成功の目安・注意点を具体化します。

### 8-1. 1,000 件スキル検査レポート + スキル安全検査コマンド

**目的**
「実在するスキルはどれくらい危ないのか」を Marmo が最初に数字で示し、同時に「自分のスキルも同じ検査ができる道具」を渡す。認知獲得と利用者獲得を一度に行う。

**すでにあるもの**
- 収集済みの実在スキル約 1,000 件（GitHub から許諾の広いライセンスのものだけを集めたもの）と、その収集手順。
- SKILL.md を読み込んで共通の形に変える機能。
- なりすまし指示の検出器（「以前の指示を無視せよ」「隠された指示を漏らせ」「道具にこう命じよ」「外へ送り出せ」の 4 系統）。
- 危険操作の検出器（外部通信先の許可・拒否、シェルの破壊的命令、データベースの破壊的命令、機密情報の持ち出し）。
- 副作用・必要権限・信頼度の区分と、それを判定する関門。

**足りないもの**
1. 検査そのものを 1 コマンドにまとめた入口（今は実行の途中で使われる部品で、単体では呼べない）。
2. スキルに同梱された補助ファイル（スクリプト・設定・URL）まで見る検査。今は SKILL.md 本文が主な対象。
3. 「未申告の外部通信」「秘密情報の参照」「破壊的コマンド」「隠し文字（見えない文字や極小の指示）」など、レポートで使う判定項目の追加と、それぞれの重み付け。
4. 点数と一言の結論（例: 「入れてよい／条件付き／入れない」）を出す採点表と、その根拠を説明する文章。
5. 人の目による確認。機械の判定だけで「危険」と公表すると誤報のリスクがあるため、上位の疑わしい例は人が読んで裏を取る。
6. 収集元のライセンス表示と著作権表示を自動でまとめる処理（公開の前提）。
7. 出力形式: 端末表示、JSON、共有しやすい 1 枚の画像または短い HTML。

**手順（提案）**
1. 検査コマンドの骨格を作る。フォルダ・ファイル・URL（GitHub のスキルの場所や配布所の URL）を受け取り、既存の 2 つの検出器と関門の判定を通し、結果を一覧にする。
2. 判定項目を増やす。同梱スクリプト内のシェル命令、環境変数や鍵ファイルへの参照、外部 URL の抽出、宣言されていない権限、隠し文字。
3. 採点表を決める。「即座に危険（拒否相当）」「人が確認すべき（保留相当）」「注意喚起」「問題なし」の 4 段階に丸め、各項目の根拠を 1 行で添える。
4. 1,000 件に一括適用し、集計する。危険度の分布、種類ごとの割合、収集元の傾向、代表例（匿名化したうえで）。
5. 上位の疑わしい例を人が読んで確認し、誤検出の割合を測る。この数字自体もレポートに載せる（信頼性の担保になる）。
6. レポートを書く。英日併記。冒頭に 3 つの数字、次に代表例、次に「どう守るか」、最後に「自分のスキルを検査する 1 行」。
7. 公開を同時に行う: 検査コマンドの入った版、レポート、収集元一覧と権利表示、ベンチマークのデータセット化（第 2 章）。

**成果物**
- コマンド 1 つ（例: スキルの場所を渡して検査、点数と根拠、JSON 出力、共有用の 1 枚）。
- 調査レポート（英日）。
- 検査項目の一覧と採点基準の文書（他者が検証できるように公開）。
- 誤検出率の測定結果。

**成功の目安**
- レポート公開後 1 か月で、検査コマンドの実行数（任意の匿名統計、または PyPI の取得数）と、OpenClaw / Claude Code 関連コミュニティでの言及数。
- 外部の記事・動画に引用される数字が 1 つ以上できること。
- 検査で見つかった問題を作者に報告し、修正された事例が数件出ること（コミュニティに貢献した実績になる）。

**注意点**
- 第 3 章先頭の安全修正（自己申告の参照・信頼度の信用、参照先ファイルの範囲）を済ませてから出す。検査道具自身に穴があると信用を失う。
- 「危険」と名指しするときは、作者の意図と悪意を区別する。多くは不注意なので、公表前に作者へ連絡する猶予期間を設けるのが慣例。
- 検出器は定型パターン照合が中心なので、すり抜けは必ずある。「安全を保証する」ではなく「明らかな問題を実行前に見つける」と説明する。

**規模感**
コマンド化と項目追加で 2〜3 週間、一括検査と人の確認で 1 週間、レポート執筆と公開準備で 1〜2 週間。合計 1〜1.5 か月程度（1 名想定）。

### 8-2. フライトレコーダー画面 + スマホ承認

**目的**
Marmo の強み「何をされたか全部見える・止められる・戻せる」を、説明ではなく体験と映像で伝える。技術者向けデモの主役と、企業の「見える化」要求への回答を同時に作る。

**すでにあるもの**
- 監査記録: 検索 → 選択 → 関門 → 起動 → 文脈組み立て → モデル呼び出し → 実行 → 復旧 → 人間確認の各段階が、1 つの追跡番号のもとに改ざん検知付きで記録され、JSON 行形式で保存できる。
- 状態保存: すべての出来事が順番に残り、任意の時点を再生でき、検査点への巻き戻しができる。
- 人間確認の仕組み: 「確認依頼」に何を・なぜ・影響・代替案・推奨が入り、応答は「承認・却下・保留・条件付き（引数を変えて承認）」の 4 種。応答を外部から受け取る呼び出し口と、保留のまま後で再開する動きがある。承認者の制限と時間切れの扱いもある。
- コマンドラインからの実行・再開・一覧表示。

**足りないもの（フライトレコーダー）**
1. 監査記録と状態保存を読み込んで、時系列の 1 枚の画面にする表示部分（ブラウザで開くだけの HTML 1 ファイルが望ましい。サーバー不要にすると配りやすい）。
2. 見せ方: 縦の時間軸に「段階」を並べ、関門で止まった所・人が承認した所・失敗して再試行した所・巻き戻した所を色で区別。各項目を開くと詳細（選ばれた資源、点数、判定理由、引数は伏せ字済み）が見える。
3. 「なぜこの資源が選ばれたか」「なぜ止まったか」を日常語で 1 行に要約する文章生成（判定理由はすでに記録されているので、並べ替えと言い換えが中心）。
4. 改ざん検知の結果表示（「この記録は改ざんされていません」の 1 行）。企業向けに効く。
5. 実行のたびに自動で出力する任意設定と、共有用の URL または画像。

**足りないもの（スマホ承認）**
1. 確認依頼を LINE / Slack / Telegram に送り、ボタンで応答を受け取る接続部品。応答は既存の呼び出し口に流し込むだけでよい。
2. 依頼文の整形: 「何をしようとしているか」「なぜ止めたか」「承認すると何が起きるか」「却下すると何が起きるか」を、スマホの画面 1 枚に収まる日常語で。
3. 条件付き承認の入力: 「この引数だけ変えて許可」をスマホで簡単に行う方法（選択肢提示か、短い入力）。
4. 誰が承認したかの記録と、承認者の制限（既存機能を接続部品側で使う）。
5. 時間切れの扱い（一定時間応答が無ければ保留のまま止める、または既定で却下）。

**手順（提案）**
1. フライトレコーダーの表示部分を、既存の監査記録ファイルを読むだけの HTML 1 ファイルとして作る。まず同梱サンプルの実行結果で見え方を固める。
2. 「止まった・承認した・巻き戻した」の 3 場面が目立つ配色と要約文を作り、社内外の非技術者に見せて分かるか確かめる。
3. Slack 接続部品を先に作る（開発者の利用が多く、ボタン付き通知が作りやすい）。次に LINE（日本市場向け）、Telegram（OpenClaw 利用者向け）。
4. 一本のデモ動画の台本を作る: 「個人秘書に頼む → 外から来た内容に怪しい指示 → 関門が止める → スマホに通知 → 却下または条件付き承認 → 再開 → フライトレコーダーで全部を振り返る → 必要なら巻き戻す」。
5. README の冒頭と案内ページに、動画と画面の静止画を置く。

**成果物**
- ブラウザで開くだけの実行記録ビューア（HTML 1 ファイル）と、実行後に自動出力する設定。
- Slack / LINE / Telegram の承認接続部品（任意インストール）。
- 90 秒程度のデモ動画と台本、静止画。
- 企業向けの「見える化・改ざん検知」説明 1 ページ。

**成功の目安**
- 動画の再生数と、そこから README への流入。
- 「止まって、スマホで承認して、再開する」場面を見た人が Marmo を一言で説明できるか（数人に見せて確かめる）。
- 企業の試験導入の打ち合わせで、フライトレコーダーの画面がそのまま資料に使えること。

**注意点**
- 画面や通知に出す情報は、監査記録の伏せ字（秘密情報の隠蔽）を必ず経由させる。見せる機能ほど漏れやすい。
- 承認の通知は「承認疲れ」を起こさない頻度に抑える必要がある。第 3 章の「なりすまし指示対策の粒度」の改善（汚染された引数を含む呼び出しだけ確認に回す）を先に、または同時に進める。
- チャットアプリの接続部品は外部サービスの仕様変更に弱いので、任意インストールの別パッケージにして本体の低依存方針を守る。

**規模感**
フライトレコーダー 2〜3 週間、Slack 承認 1 週間、LINE / Telegram 各 1 週間、デモ動画と資料 1 週間。合計 1.5〜2 か月程度（1 名想定）。8-1 と並行するなら、先に 8-1 を終えてから 8-2 に入るのが、公開の勢いをつなげやすい順序です。
